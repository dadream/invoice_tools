# 发票报销 AI 助手 - 产品方案

> **定位**：把散落在邮箱里的发票，变成按行程归好、查过重、排好版、已进 Concur 收据库的批次
> **技术栈**：Tauri 桌面应用 + DeepSeek V4 Flash + 本地 PaddleOCR
> **最后更新**：2026-08-03

---

## 目录

1. [产品概述](#一产品概述)
2. [核心价值与折旧曲线](#二核心价值与折旧曲线)
3. [业务流程](#三业务流程)
4. [技术架构](#四技术架构)
5. [功能模块](#五功能模块)
6. [MVP 范围](#六mvp-范围)
7. [计费方案](#七计费方案)
8. [用户体验](#八用户体验)
9. [观测指标](#九观测指标)
10. [测试计划](#十测试计划)
11. [增长策略](#十一增长策略)
12. [风险与合规](#十二风险与合规)
13. [路线图](#十三路线图)
14. [明确不做的方向](#十四明确不做的方向)
15. [附录：竞品调查](#附录竞品调查)

---

## 一、产品概述

### 1.1 问题定义

针对**使用 Concur 报销的差旅人群**：

| 环节 | 现状痛点 | 时间消耗 |
|---|---|---|
| 平台开票 | 逐平台手动开票 | 30 分钟 |
| 邮箱下载 | 手动搜索、下载发票 | 20 分钟 |
| 分类整理 | 按行程归组、文件夹分类 | 40 分钟 |
| 打印整理 | 排版、打印、粘贴、装订 | 60 分钟 |
| 录入提交 | Concur 逐条录入 | 90 分钟 |
| **合计** | | **1-2 天** |

退单返工的隐性成本更高：重复报销、发票作废、信息错误。

### 1.2 产品定位

把"邮箱里散落的一堆发票"变成"按行程归好、查过重、排好版、已进 Concur 收据库"的批次。

**对外定位保持 Concur-only**——"用 Concur 报销的人，装这个"。这句话能传播，通用工具定位不能。

**架构上保持系统无关**：核心数据模型不含任何 Concur 概念，输出层是可插拔适配器。

### 1.3 目标用户

**筛选口径是单次发票张数，不是出差趟数。**

出差趟数只是发票张数的不精确代理：一趟 10 天跨 3 城的长差有 40+ 张票，比两趟短差更痛。

| | 定义 |
|---|---|
| **核心用户** | 单次报销 ≥15 张发票 + 公司用 Concur |
| **拓展维度** | 代报同事的行政/助理（需 `报销人` 字段隔离台账） |

**已知的次优细分**（暂不主攻）：销售/BD 岗——发票张数更多（40-80）但单张金额低，且**几乎不需要行程归组**（我们最难的模块）。若差旅人群验证不顺，这是备选切入点。

### 1.4 商业模式

- **个人版**：按量付费 + 包月/包年订阅
- **无企业版**（已决策）

无企业版意味着收入天花板明确：

| | 付费用户 | 年收入 |
|---|---|---|
| 乐观 | 5,000 | ~¥75 万 |
| 中性 | 1,500 | ~¥22 万 |

这是"1-2 人做一个能养活自己的产品"。它决定了后续每个功能都要问：值得用有限时间做吗？

**推论**：LTV 约 ¥300，付费获客不可行。**内容营销 + 口碑是唯一渠道**，因此病毒机制不是可选项。

### 1.5 待验证的关键假设

| 假设 | 状态 | 验证方式 |
|---|---|---|
| Concur 中国的用户池足够大 | ⚠️ **未核实** | β 用户来源分布反推 |
| 无直接竞品 | ⚠️ 证据不足 | 见[附录](#附录竞品调查) |
| 公司邮箱允许 IMAP | ⚠️ 存疑（Office365 常禁用） | β 招募问卷 |
| 公司仍要求纸质件 | ⚠️ 存疑 | β 招募问卷 |

前两项若为负，需重新评估产品是否成立。

---

## 二、核心价值与折旧曲线

### 2.1 价值主张

| 维度 | 目标 |
|---|---|
| 端到端时间 | 1-2 天 → 20 分钟 |
| 自动通过率 | ≥85% |
| 归组准确率 | 平均人工调整 < 3 张/批次 |
| 风险防范 | 跨月重复报销检测、作废票识别、规则合规 |

### 2.2 折旧曲线（资源分配的核心依据）

产品价值建立在世界当前的一个**过渡状态**上，而这个状态的四个支柱都在侵蚀：

| 支柱 | 趋势 | 受影响能力 |
|---|---|---|
| 发票格式混乱 | 数电票已基本普及（铁路 2024/11、航空 2024/12 起） | 解析、OCR |
| 公司要求纸质件 | 电子归档标准推进中 | **打印排版** |
| Concur 不懂中国发票 | 不确定，SAP 可能补上 | Concur 对接 |
| 行程归组需要判断 | **不会被数字化解决** | 归组 |

```
快速折旧 ──────────────────────────→ 长期保值
打印排版    OCR/解析    Concur对接    行程归组    跨月台账
```

**两条资源分配原则**：

1. **打印模块做够用就停**——它现在省时最多（60 分钟）但折旧最快。不做智能排版、不做按票据尺寸自适应。
2. **归组与台账是长期资产**——归组是唯一不会被数字化消解的能力；台账价值随使用时间线性增长，且是用户数据，构成迁移成本。

### 2.3 AI 趋势是净利好

技术趋势侵蚀**价值**，但改善**实现**：

| 变化 | 影响 |
|---|---|
| 视觉模型普及 | OCR 流水线可能塌缩成一次模型调用 |
| 模型成本下降 | COGS 继续走低 |
| Agent 可靠性提升 | 方案 C 能自适应 UI 变化 |

因此 **OCR 层必须设计成可替换**（见 4.3）。

---

## 三、业务流程

### 3.1 流程重构

| 环节 | 现状 | 产品方案 | 人工参与 |
|---|---|---|---|
| 1 平台开票 | 手动逐平台开 | v0.5 产出开票缺口清单 | 人工补开 |
| 2 邮箱下载 | 手动搜索下载 | IMAP 自动抓取 + 幂等去重 | 0 |
| 3 分类整理 | 人工按行程分 | 确定性算法 + LLM 兜底歧义 | 审核时可调 |
| 4 打印整理 | 人工排版粘贴 | A4 合订本 PDF，一键打印 | 打印 + 装订 |
| 5 核对录入 | 逐条录 Concur | 收据入库（B）+ 表单填写（C，v0.5） | 审核 + 点提交 |

### 3.2 六层流水线

```
采集 → 解析 → 校验 → 归组 → 审核 → 输出
IMAP   格式路由  验签去重  行程重建  单屏确认  目录/打印/Concur
```

贯穿全部的原则：**确定性优先，LLM 兜底**。能用规则、正则、验签解决的绝不调模型；LLM 只处理歧义。这是成本能压到 COGS ¥1.81 的根本原因。

---

## 四、技术架构

### 4.1 Tauri 桌面应用 + 本地优先

**已定：Tauri**（非 Electron）。理由：体积 ~10MB vs ~120MB（首次下载的第一印象）、内存与启动更优、Rust 后端默认隔离更安全。代价是跨平台一致性需多测（Windows WebView2 / macOS WebKit）。

**为什么不是 Web 应用**——三个技术硬约束：

| 约束 | 说明 |
|---|---|
| 浏览器无法直连 IMAP | Web 方案必须由服务器代抓邮件，邮箱凭证必须过服务器 |
| 本地 OCR 无法在浏览器跑 | PaddleOCR 需原生运行时，Web 只能上传图片到服务端 |
| 方案 C 必须在用户本地 | 用用户登录态操作才是"辅助"而非"代理"，这是合规底线 |

**承认的假设**：用户在意"邮箱凭证不出本地"。如果实际不在意，桌面应用的安装门槛就是纯成本。观测点见第九节 #2。

### 4.2 技术选型

| 层 | 选型 | 说明 |
|---|---|---|
| 桌面框架 | **Tauri** | 已定 |
| 本地存储 | SQLite（加密） | 密钥由系统 Keychain 派生 |
| 邮件 | IMAP（App 专用密码优先，OAuth 延后） | |
| 解析 | lxml / ofdparser / pdfplumber | 数电票 XML、OFD、PDF 文本层 |
| OCR | **PaddleOCR（本地）** | MVP 仅本地；云兜底延后 |
| LLM | DeepSeek V4 Flash | 仅归组歧义、邮件判定、类型映射 |
| 浏览器自动化 | Playwright（v0.5） | 本地运行，非 headless |
| 打印 | ReportLab / WeasyPrint | A4 合订本 |

### 4.3 三条架构纪律

这三条成本接近零，但决定了后期的可选性。

**纪律 1：归组引擎与 UI 解耦**

归组必须是可独立调用的模块：输入（发票列表 + 常驻城市 + 配置）→ 输出（行程分组 + 置信度）。不允许归组逻辑散落在界面代码里。

理由：归组引擎是长期资产（见 2.2），未来可能作为 SDK 授权给国内报销 SaaS。耦合进 UI 就拆不出来。

**纪律 2：OCR 层可替换**

现在是「PaddleOCR + 票种模板」，未来可能是「图片 → 视觉模型 → 结构化字段」一步到底。票种模板逻辑必须集中，不散落。

**纪律 3：核心数据模型不含任何 Concur 概念**

```typescript
interface Invoice {
  票种: '铁路' | '航空' | '酒店' | '市内交通' | '餐饮' | '其他'
  日期: string; 金额: number; 税额: number; 税率: number
  发票号: string; 行程归属: string | null; 报销人: string
  // ❌ 不出现 expenseType / reportHeader / itemization
}

interface ReimbursementAdapter {
  name: string
  capabilities: {
    上传收据: boolean; 创建报告: boolean
    填写条目: boolean; 关联收据: boolean
    提交: false        // 永远 false，提交只能由人做
  }
  映射费用类型(invoice: Invoice): string   // Concur 词汇只活在适配器内
  上传收据?(files: RenderedInvoice[]): Promise<Result>
  创建报告?(batch: Batch): Promise<ReportRef>
  填写条目?(report: ReportRef, entries: Entry[]): Promise<Result>
  关联收据?(report: ReportRef, links: Link[]): Promise<Result>
  校验(report: ReportRef, expected: Batch): Promise<VerifyResult>
}
```

**MVP 实现两个适配器**：

| 适配器 | capabilities | 用途 |
|---|---|---|
| `ConcurAdapter` | 上传收据: true，其余 false | 方案 B |
| `GenericAdapter` | 全部 false | 纯本地模式：只输出 Excel/CSV + 打印件 |

第二个几乎零成本（Excel 输出本来要做），但它是抽象是否成立的唯一验证方式。只实现一个适配器，接口最后一定会长成 Concur 的形状。

用户在设置里选择模式，两种都要支持。

### 4.4 数据与隐私

**数据分级**：

| 数据 | 敏感度 | 位置 |
|---|---|---|
| 邮箱凭证 | 极高 | 仅本地，AES-256-GCM 加密 |
| 发票原件 / 提取字段 | 高 | 仅本地 |
| 台账（发票号 + 报销日期） | 中 | 本地，可导出 |
| 配置（常驻城市、抬头税号、费用类型映射） | 中 | 仅本地 |
| 匿名遥测（张数、票种分布、调整次数） | 低 | 可选上传，**默认关闭** |
| 账号与余额 | 低 | 云端（计费必需） |

**加密密钥来自系统 Keychain**，用户无感，重装不丢：

- macOS → Keychain
- Windows → DPAPI
- Linux → Secret Service / libsecret

**本地存储结构**：

```
~/.invoice-assistant/
├─ accounts.db     账号、余额、邮箱凭证（加密）、设置
├─ ledger.db       发票、批次、台账（reported_invoices）
└─ files/{batch_id}/{originals,rendered,outputs}/
```

**匿名遥测只发结构不发内容**。可以发：张数、票种分布、解析层级分布、归组调整次数、耗时。绝不发：金额、日期、地名、发票号、买卖方名称。

**用户数据权利**：导出全部数据（ZIP）、删除账号、清空台账（换公司场景）、**导出/导入台账**（最小跨设备能力，只同步 `reported_invoices`，不同步批次文件）。

**服务器被攻破的暴露面**：仅账号邮箱、余额、匿名统计。不含发票内容与邮箱凭证。这是本地优先的真正价值——不是零风险，而是风险边界可控。

**隐私政策必须列明的第三方**：DeepSeek API（压缩后的行程事件，不含金额）、支付渠道。v0.5 引入云 OCR 后追加腾讯云/讯飞（会拿到完整发票图，需给用户本地/云端二选一）。

### 4.5 成本结构

单批次（50 张典型，MVP 无云 OCR、无付费查验）：

| 成本项 | 小计 |
|---|---|
| LLM（DeepSeek V4 Flash，归组歧义 + 邮件判定） | ¥0.06 |
| 本地 OCR（PaddleOCR） | ¥0 |
| 本地验签 | ¥0 |
| 存储 + 带宽 | ¥0.10 |
| **合计 COGS** | **≈¥0.16** |

MVP 阶段实收 ¥10（50 张 / ¥8,000）→ 毛利 **98%**。

v0.5 引入云 OCR（¥0.09/次）和付费查验（¥0.30/次）后，典型批次 COGS 升至约 ¥1.81，毛利 82%。配额机制默认关闭，视运营数据决定是否启用。详见计费方案文档。

---

## 五、功能模块

### M1 邮件采集

**IMAP 自动抓取是主通道**，三级漏斗避免 LLM 逐封读邮件：

1. **发件人白名单**（确定性，覆盖约 85%）：12306、航司与航旅纵横、携程/飞猪/同程、滴滴、高德、美团、连锁酒店集团、各地税务局电子发票平台。
   - **白名单云端可更新**：内置默认列表 + 启动时拉取远端 JSON，避免每加一个平台就要发版
2. **附件特征**（确定性）：`.xml` / `.ofd` / `.pdf` 且文件名或首页含"发票""行程单""结算单"
3. **LLM 判定**（仅剩余歧义邮件，约 5–10 封/批次）：发件人 + 主题 + 附件名 + 正文前 200 字 → `{is_invoice, 票种猜测, 置信度}`

**幂等去重三重键**：`Message-ID` + 文件 SHA256 + 发票号码。同一张票被平台重发、用户转发，收敛为一条。

**兜底通道**：`.eml` / 文件夹拖入 / 手机截图。

> ⚠️ **已知风险**：外企常用 Office365，部分公司的条件访问策略禁用 IMAP/基本认证。应对：提供各邮箱的图文配置教程（重点讲"应用专用密码"不是登录密码）+ 文件夹导入兜底。观测指标 #2 跟踪邮箱配置成功率，低于 70% 时重新评估通道优先级。

### M2 解析：格式路由

按数据质量分级，能停在上层绝不下沉：

| 级别 | 来源 | 方法 | 准确率 | MVP |
|---|---|---|---|---|
| **L0** | 数电票 XML、OFD 内嵌 XML、PDF 附带 XML | 直接读结构化字段 | ~100% | ✅ |
| **L1** | PDF 文本层（含铁路/航空行程单） | pdfplumber + 票种版式模板 | >99% | ✅ |
| **L2** | 图片、扫描件、照片 | 本地 PaddleOCR + 增值税票结构化提取 | 92–95% | ✅ |
| L3 | L2 置信度 < 0.75 | 云 OCR（腾讯云/讯飞） | 96–99% | ❌ v0.5 |
| **L4** | 关键字段冲突 | **强制人工** | — | ✅ |

**为什么 L2 必须进 MVP**：Concur 填表需要税率和税额。数电票 XML 能直接读，但**图片格式的增值税票（定额发票、餐饮/酒店纸票扫描件）只有图**，不 OCR 就拿不到税额。差旅人群这类票约占 15–25%。

**OFD 解析**复用开源实现（`ofdparser`、`invoice-ofd2json` 一类），含验签与内嵌发票信息提取。

**架构纪律**：票种模板集中管理，不散落各处。未来视觉模型成熟后，L2/L3 可能塌缩为单次模型调用。

**MVP 降级路径**：PaddleOCR 置信度低的票，不调云端，直接进"手工录入"队列。观测指标 #3 跟踪手工录入占比，超过 20% 则 v0.5 加云 OCR 兜底。

### M3 校验与去重

全部确定性，零 LLM 成本：

- **结构校验**：发票号码格式、日期合法性、税率合规、价税合计恒等式
- **真伪**：数电票 XML/OFD 数字签章 **本地验签**（免费，等价于查验）
- **本批去重**：发票号码为主键
- **跨月重复报销检测**：本地 SQLite 常驻台账。**这是订阅的核心价值**，也是长期资产
- **规则校验**：抬头、税号、开票日期是否超出报销时限（可配置规则包）

**台账价值必须显性化**。即使未检出重复也要展示：

```
已对照 3 个月历史台账（147 张发票），未发现重复。
```

否则用户感受不到这个检查发生了。

**付费查验**（¥0.30/次）不进 MVP。它的目的不是防伪（用户没有伪造动机），而是识别**已作废/红冲但签章依然有效**的发票——这是本地验签唯一拦不住的失败模式。v0.5 引入，仅在"平台重发迹象 / 金额与明细不符 / 开票超 60 天"等信号命中时触发。

---

### M4 行程归组（核心差异化 + 长期资产）

**常驻城市**：用户在设置里直接填写，支持多个（如常驻北京但长期驻场上海）。不做自动推断。

**算法流程**：

```
Step 1  骨架：取所有城际交通票，按出发时刻排序
Step 2  切分：从常驻城市出发 = 行程起点；回到常驻城市 = 终点；
        中间的票 = 中途转城 → trip{start, end, city_chain}
Step 3  挂住宿：入住日期 ∈ [start, end] 且城市 ∈ city_chain
        （用 checkin_date，不用 invoice_date——酒店常延迟开票）
Step 4  挂零散：时间 ∈ [start−6h, end+6h] 且城市 ∈ city_chain
        ∪ {常驻城市的机场/车站往返段}
Step 5  残余 → 按月归入"市内消费"桶
Step 6  歧义 → 一次 LLM 批量调用
Step 7  缺票检测
```

**确定性可解的场景**（不调 LLM）：同日多城、跨国出差、火车中转 < 4h、去机场的市内票、酒店延迟开票、纯市内出差。

**必须交给 LLM 的 5 类歧义**：

1. 无返程票 + 下一趟起点不同
2. 周末夹在两趟中间且无明确返程
3. 中转停留 4–12 小时（< 4h 判中转，> 12h 判行程点，中间问 LLM）
4. 同一城市短期内多次往返
5. 时间重叠（两张票显示同时在两个城市）

**LLM 调用设计**：一次调用处理本批次全部歧义。50 张发票压缩成 5–10 个"待判定事件"，每个约 100 tokens，总输入约 2K tokens，成本约 ¥0.01。

```json
{
  "常驻城市": "北京",
  "待判定事件": [{
    "类型": "无返程票",
    "描述": "7月3日去上海（G13），无返程。7月10日去深圳（CZ3001）。",
    "候选": ["7/3–7/9 一趟上海", "7/3–7/5 上海 + 7/10–7/12 深圳两趟"],
    "涉及发票ID": [1, 2, 3, 15, 16]
  }]
}
```

返回每个事件的选择 + 置信度 + 理由。低置信度的进人工审核。

**准确率推导**：

```
确定性覆盖 70% × 98%  +  LLM 歧义 25% × 85%  +  极端 5% × 50%  ≈  92.4%
```

**α 验收线：平均调整 < 3 张/批次**（等价于准确率 ≥ 94%）。这比推导值更严格，意味着两处必须做得比基线好：
- 确定性覆盖率往 80% 推（把中转、机场往返等规则写死）
- LLM 判对率验证到 85% 以上

不达标时的处理顺序：先看调整类型分布——集中在少数几类 → 补规则；分散在各类型 → 算法框架回炉。

**已知限制**（文档明示，不在 MVP 解决）：
- 多人同行票（家属机票、同事酒店）无法识别，需用户拖入"排除"桶
- 境外城市/地区识别可能不准（香港、新加坡、台北按地区处理）

**学习功能**（记住用户的调整偏好）延后到 v0.5——需要先积累样本，判断用户是否真在重复调整同一类问题。

**架构纪律**：归组引擎与 UI 解耦，输入「发票列表 + 常驻城市」，输出「行程分组」，可独立调用。

### M5 人工审核（唯一人工卡点）

**单屏布局**：左侧行程分组树，右侧发票列表（按选中行程过滤）+ 缩略图 + 提取字段，异常项红标。

**调整操作**：拖拽移动、多选批量移动、行程拆分、行程合并、排除。

> 如果用户频繁使用"拆分/合并"，说明算法切分逻辑有问题——α 阶段的重点观察项。

**只在四种情况打断用户**，其余自动通过：

1. 关键字段（金额/号码/日期）置信度低于阈值
2. 规则冲突（抬头错、超期、疑似重复）
3. 单张金额超用户设定阈值（默认 ¥2000）
4. 归组歧义或 LLM 低置信

**自动通过率目标 ≥ 85%**。低于此用户会觉得"还不如自己弄"。

**5 分钟目标拆解**：

| 动作 | 数量 | 单次 | 小计 |
|---|---|---|---|
| 浏览行程分组 | 3 趟 | 15s | 45s |
| 处理待确认项 | 3 张 | 25s | 75s |
| 手工录入纸票税额 | 5 张 | 20s | 100s |
| 检查金额合计 | 1 次 | 30s | 30s |
| **合计** | | | **~4.2 min** |

**纸票录入界面单独设计**：它占审核时间的 40%，但容易被忽略。要求紧凑——图在左，金额/税额/日期三个输入框在右，Tab 切换，Enter 提交并跳下一张。不能用弹窗式逐个编辑。

#### 防"静默的错"（比报错更危险）

比"报错"更糟的是**看起来正常但值是错的**——OCR 把 ¥128.00 读成 ¥12.80 且置信度 0.85，或酒店票挂错行程。后果是提交后被财务退单，代价远高于我们省下的时间。

三个防护：

| 防护 | 做法 |
|---|---|
| **金额合计强制对账** | 显示总额 + 各行程分项。用户对自己的月度花费有预期，一眼能看出量级错误 |
| **OCR 必须显示原图对照** | 不只显示提取结果，原图缩略图放旁边，扫一眼能发现 ¥12.80 vs ¥128.00 |
| **打印件页数校验** | 生成后校验页数与预期一致，首页打印目录（第几页是哪张票） |

---

### M6 目录整理与打印

**标准目录结构**：

```
2026-07报销批次/
├── 00_报销单/
│   ├── 报销汇总表.xlsx        ← 含"Concur 费用类型"映射列
│   ├── Concur录入清单.csv
│   └── 打印合订本.pdf
├── 01_出差_0703-0705_上海/
│   ├── 00_行程说明.md
│   ├── 10_交通/  20_住宿/  30_市内交通/
│   └── 99_原始件/            ← 永久保留，任何加工不覆盖原文件
├── 02_出差_0715-0719_深圳-广州/
├── 90_市内_2026-07/
└── 91_待确认/                ← 需人工处理的全在这
```

命名格式：`{日期}_{类型}_{摘要}_{金额}_{票种}.{ext}`

**汇总表留费用类型映射列**：输出我们的标准类型（铁路/航空/酒店…），旁边留一列供用户填写自己公司 Concur 的费用类型代码。用户填一次可复用，也可导出给同事。v0.5 做方案 C 时这个映射直接复用。

**打印排版引擎（做够用就停，不做智能排版）**：
- 火车票/机票行程单独占页
- 出租车小票 6 张/页，每张标序号
- 附页码 + 与汇总表行号对应
- 首页打印目录

目标：人只需顺序装订，不需要判断。

> **折旧提醒**：这是折旧最快的模块。观测指标 #6（打印件下载率）低于 70% 即降优先级。

### M7 Concur 提交

方案 A（Expense API，需企业授权）和方案 D（批量导入，权限在管理员手里）已排除。个人用户的现实路径只有 B 和 C。

| 方案 | 能力 | 风险 | 定位 |
|---|---|---|---|
| **B. 邮件收单** | 收据入 Available Receipts | **零风险** | **地基，MVP 必做** |
| **C. 浏览器自动化** | 建报告、填条目、关联收据 | 需确认 IT 政策 | 主力，v0.5 |

**为什么 B 和 C 的风险等级完全不同**：

| | B（邮件） | C（浏览器自动化） |
|---|---|---|
| 依赖 | SMTP + 一个地址 | Concur 页面 DOM 结构 |
| Concur 改版影响 | 几乎为零 | 每次 UI 更新都可能挂 |
| 失效表现 | 不会失效 | 找不到元素，或更糟：填错位置 |
| 维护成本 | 零 | 持续 |

`receipts@concur.com` 是存在十几年的公开产品功能，不随前端改版消失。方案 C 依赖 SPA 结构，而 Concur 在推 NextGen UI，新旧界面并存。所以 C 的真实成本是"开发 + 长期维护"。

#### M7-B 邮件收单（MVP）

```
渲染发票为 PDF/PNG（XML/OFD 必须转换，Concur 不认这两种格式）
  ↓
用户在 Concur Profile 验证过的邮箱发信 → receipts@concur.com
  ↓
Concur 靠发件人邮箱认人 → 收据进入 Available Receipts
  ↓
C 阶段（或用户手工）从收据库关联到费用条目
```

**格式约束**（必须落地）：

| 约束 | 处理 |
|---|---|
| 只接受 PDF/JPG/JPEG/PNG/HTML/TIF/TIFF | XML/OFD 渲染成 PDF（打印模块已有此能力，复用） |
| 每文件 ≤ 5MB | 发送前压缩，建议黑白、≤1024×768 |
| 每封 ≤ 10 个附件 | 50 张拆 5 封，带重试队列 |

**结构化文件名**（Concur 显示的就是附件名）：

```
{日期}_{类型}_{金额}_{发票号后6位}.pdf

20260703_Rail_553.00_345678.pdf
20260703_Hotel_1280.00_234567.pdf
```

**不加批次序号**——序号只在批次内唯一，Available Receipts 是全局库，12 次报销会有 12 个 `001_`。日期 + 金额 + 发票号后 6 位已足够全局唯一（撞号概率 < 0.01%，且撞了会被匹配逻辑检测为"多个候选"并留给用户）。

三个好处：C 阶段精确匹配；自动化失败时用户手工挑也能一眼认出；同名文件立即暴露重复发送。

**首次使用先发 1 张试探**：部分公司在租户级强制启用了 ExpenseIt，它会自动 OCR 并创建费用条目——而它对中国发票不可靠，会生成一堆字段错误的条目。

```
发 1 张 → 用户去 Concur 看结果
  ├─ 只进 Available Receipts → 正常，批量发
  └─ 自动生成了条目 → 提示"您公司启用了 ExpenseIt，
                        自动条目字段可能不准，建议删除后手工填写"
                        并记录该用户的租户特性
```

**必做的前置引导**：验证 Concur 邮箱是方案 B 的唯一前置条件，需配 3 张截图的图文教程。

**每张发票独立文件，不发合订本**——Concur 里每个费用条目要关联自己的收据，合订本会导致所有条目共享一个 PDF，多数公司财务不接受。合订本只用于打印邮寄。

#### M7-C 浏览器自动化（v0.5）

**合规边界（写死在产品里）**：

| 约束 | 做法 |
|---|---|
| 运行位置 | Playwright 在用户本地，不经过我们的服务器 |
| 登录 | 用户手动完成 SSO + MFA |
| 凭证 | 不存储，会话只在本地内存 |
| 可见性 | 浏览器窗口可见（非 headless） |
| 最终动作 | **提交按钮由用户点击**，工具只填到 Draft |
| 授权 | 首次启用要求用户确认公司 IT 政策允许 |

定位是"本地辅助填写，用户在场且可见"，不是无人值守机器人。

**C0 配置探测（关键设计）**：Concur 是高度可配置的 SaaS，不可能写一套通用脚本。

| 可配置项 | 各公司差异 |
|---|---|
| 费用类型 | A 公司 "Rail"，B 公司 "TRV-RAIL-CN" |
| 必填字段 | 成本中心 / 项目号 / 客户代码，各不相同 |
| 审批流 | 有的要先关联出差申请（Travel Request） |
| UI 版本 | 旧版 / NextGen，选择器完全不同 |
| 界面语言 | 中文 / 英文 |

```
首次运行 → 探测该用户的 Concur 配置
  ├─ 抓取可选费用类型列表 → 用户确认映射
  ├─ 抓取报告头必填字段 → 用户填一次模板
  ├─ 检测 UI 版本 → 选择对应选择器集
  └─ 保存为"公司配置档案"（约 5 分钟一次性成本）
后续运行 → 用档案填表
```

**执行模式：批量建完 + 集中检查**（不是逐条等确认）

```
批量自动化（5-10 分钟，用户可见但无需参与）
  建报告 → 批量建 50 个条目 → 关联收据 → 自我校验 → 停在 Draft
    ↓
集中检查（2-3 分钟）
  工具显示对比结果 → 用户在 Concur 里滚动确认 → 处理异常 → 点 Submit
```

逐条确认要 30 分钟，等于把自动化的价值打了 95% 折扣。批量建完还能做完整性校验（条目数、总额回读比对），中断后靠幂等检查（报告名 + 条目指纹）重跑不会重复创建。

**分阶段落地，每级可独立降级**：

| 阶段 | 做什么 | 失效后果 |
|---|---|---|
| C1 | 建报告 + 填报告头 | 用户手工建报告 |
| C2 | 批量建条目 + 填字段 | 退回 C1 |
| C3 | 关联 Available Receipts | 退回 C2 |

**收据关联"尽力而为"**：文件名精确匹配 → 自动关联；日期+金额匹配但票号不符 → 跳过标注；多个候选 → 跳过留给用户。最终明确告知哪些关联了、哪些没有。

**失效检测**：找不到预期元素时立即停下并明确告知"Concur 界面可能已更新"，绝不猜测着继续点。自动化的失败模式不是报错退出，而是"填错了但看起来正常"。

### M8 发票替换（v0.5）

唯一允许的"修改"操作：用户提供新文件 → 重新走完整解析校验 → 替换旧记录 → 保留双版本可追溯。旧文件移入 `99_原始件/replaced/` 永久保留，替换动作写审计日志。界面用 🔄 标记 + 替换历史面板（可恢复旧版本）。

约束：只能同类型替换（跨类型是删除+新增）；解析失败回退旧版本；单批次替换上限 20 次。

MVP 阶段用"删掉重跑"替代。

---

## 六、MVP 范围

首批用户明确为**差旅人群 + 使用 Concur**，MVP 目标是跑通完整流程并验证付费。

### 6.1 做什么

| 模块 | MVP 范围 |
|---|---|
| M1 邮件采集 | IMAP（App 专用密码优先，OAuth 延后）+ `.eml`/文件夹拖入兜底 |
| M2 解析 | L0/L1 全部 + **L2 本地 PaddleOCR（仅增值税票）** |
| M3 校验去重 | 本地验签 + 发票号去重 + **跨月台账** + 基础规则 |
| M4 行程归组 | 确定性算法 + 一次 LLM 批量歧义判定 |
| M5 审核界面 | 简化版：列表 + 编辑 + 拖动改归组 + **紧凑纸票录入界面** |
| M6 输出 | 标准目录 + Excel 汇总（含费用类型映射列）+ **A4 打印合订本** + Concur CSV |
| M7-B | **Concur 邮件收单**（结构化文件名 + 先发一张试探） |
| 适配器 | **Concur + Generic 两个**（验证抽象不被污染） |
| 模式 | **纯本地模式 + Concur 模式**，用户在设置里选 |
| 计费 | 账号 + ¥20 注册赠送 + 余额扣费 + **手工充值/手工开票** |
| 基础能力 | **试运行模式** + **批次状态持久化断点续跑** + 台账导出/导入 |

**为什么这些是必做**：

- **行程归组**：选了差旅人群，这是和"文件下载器"的分界线
- **方案 B**：对外定位 Concur-only，若 v0.1 完全不碰 Concur，定位站不住、口碑传不动。成本仅约半周（PDF 渲染和结构化命名本来就要做）
- **本地 OCR**：税率和税额是 Concur 必填项，图片格式的增值税票（扫描件、定额发票、纸票照片）只能靠 OCR 提取，占比约 15–25%
- **打印合订本**：省时最多（60 分钟），用户感知最强
- **试运行模式**：让用户敢用真实历史数据试（不计费、不写台账），是"敢不敢用"到"愿不愿付"之间的桥
- **断点续跑**：50 张跑到第 40 张时 LLM 超时不能让用户从头再来。首次使用就崩溃且丢进度，基本不会有第二次
- **两个适配器**：只实现一个的话，接口最后一定长成 Concur 的形状

### 6.2 不做什么

| 模块 | 原因 | 何时做 |
|---|---|---|
| 云 OCR 兜底（腾讯云/讯飞） | 本地 PaddleOCR 先跑，看观测数据 | v0.5 |
| OCR 双通道交叉校验 | 本地置信度分级已够用 | v0.5 |
| M7-C 浏览器自动化 | 高成本 + 脆弱，需先有稳定审核界面 | v0.5 |
| 付费发票查验 | 命中率低，本地验签已覆盖数电票 | v0.5 |
| 发票替换机制 | 删掉重跑够用 | v0.5 |
| 缺票检测 | 锦上添花 | v0.5 |
| 归组学习功能 | 需先有 3 个月样本才知道哪些模式值得记 | v0.5+ |
| 移动端 | 先观察用户是否主动邮件发照片 | 看数据 |
| 云同步 | 台账导出/导入已覆盖核心需求 | v2.0 |
| 配额机制 | 已定默认关闭 | 看数据 |

### 6.3 交付给用户的完整故事

> 连上邮箱 → 自动抓 52 张发票 → 按 3 趟行程归好 → 查出 1 张上月报过的 →
> 生成打印合订本 → 52 张已进 Concur 收据库 → 拿着汇总表在 Concur 里填表提交

用户仍需手工填 Concur 表单（90 分钟里的大头留到 v0.5 解决），但前面 150 分钟压缩到 15 分钟。这个价值足够支撑 ¥10 的收费。

**必须明确告知"接下来做什么"**——MVP 没有方案 C，如果不说清楚，用户会以为工具没做完事。

---

## 七、计费方案（摘要）

详见 `invoice-reimbursement-pricing.md`。

**计费规则**：按 ¥0.20/张 计费，总费用不超过报销总额的 2‰，不足 ¥3 按 ¥3/次。

```python
实收 = max(¥3, min(张数 × ¥0.20, 报销总额 × 0.002))
```

| 场景 | 张数 | 报销总额 | 实收 |
|---|---|---|---|
| 市内月 | 25 | ¥900 | ¥3 |
| 单趟出差 | 30 | ¥5,000 | ¥6 |
| **典型出差月** | **50** | **¥8,000** | **¥10** |
| 重月 | 80 | ¥15,000 | ¥16 |

**订阅**：包月 ¥38 / 包年 ¥328。核心价值是**永久台账**（跨年重复报销检测）+ 年度差旅报告，不对外强调技术配额。场景化推荐，不强推。

**新用户**：注册赠 ¥20（约 2 个典型批次）。这意味着第 3 个月才首次付费，需在第 2 次完成后展示累计价值：

```
累计处理 97 张发票 · 节省约 5 小时 · 拦截重复 1 张（¥553 高铁票）
```

"拦截重复 1 张"比"节省 5 小时"更有说服力——它是可验证的、避免了实际损失的事件。

**订阅边界**：包月/包年是**个人订阅**，覆盖本人报销。代报多人需团队方案（暂不设计，只做检测：`报销人` 字段超 2 人时提示一次，不硬拦）。

**手工充值桥接**：微信/支付宝商户号需企业资质、周期以周计。MVP 用个人收款码 + 后台手工加余额，正式上线时再申请商户号。MVP 要验证的是"有人愿意付吗"，技术支付集成是确定能做成的事，不需要用 MVP 验证。开票同理，50 个用户手工开足够。

---

## 八、用户体验

### 8.1 首次运行

需要用户完成：安装 → 注册（拿 ¥20）→ 填常驻城市 + 抬头/税号 → 配置邮箱 → 验证 Concur 邮箱（可跳过）→ 跑第一个批次。

**邮箱配置是最大流失点**。各邮箱门槛差异很大：

| 邮箱 | 用户要做什么 | 难度 |
|---|---|---|
| Outlook/Office365（外企主流） | 可能被公司策略禁用 IMAP | ⚠️ 取决于 IT |
| Gmail | 开两步验证 → 生成应用专用密码 | 中 |
| 企业自建 Exchange | 问 IT 要 IMAP 地址 | 高 |
| QQ/163 | 设置里开 IMAP，拿授权码 | 中 |

必须配图文教程，重点讲清"应用专用密码 ≠ 登录密码"。IMAP 失败时**兜底通道不能让用户感觉是降级**——文件夹导入只多 5 分钟手工下载，后面的价值完全一样。

### 8.2 试运行模式

首次运行引导用户处理"最近一次尚未报销的发票"。若想先试历史数据，提供试运行模式：**跑完整流程但不计费、不写台账、输出文件加 `[试运行]` 前缀**。

解决三个问题：用户敢用真实数据试、我们能拿到归组反馈、不污染台账（历史已报销的票不会被记为"已报"）。

### 8.3 错误处理分三类

| 类型 | 例子 | 呈现方式 |
|---|---|---|
| **用户可修复** | IMAP 密码错、文件损坏、缺抬头 | 明确说改哪里 + 一键跳转 |
| **需要用户决策** | 归组歧义、疑似重复、OCR 置信度低 | 进审核界面，**不叫"错误"** |
| **系统故障** | LLM 超时、磁盘满、崩溃 | 说明 + 保留进度 + 重试 |

**措辞原则**：技术分级（L0/L1/L2、置信度）完全隐藏在后台。

| 不要 | 要 |
|---|---|
| "OCR 置信度 0.72，低于阈值" | "这张票的金额没看清，请确认" |
| "L2 解析失败" | "这是张图片，需要您填一下金额" |
| "IMAP 认证失败 (535)" | "邮箱密码不对。注意要用'应用专用密码'，不是登录密码" |

### 8.4 批次状态持久化

```
采集完成 → 解析完成 → 校验完成 → 归组完成 → 已输出
每步完成写 SQLite
中断后重启 → "上次的批次在【归组】步骤中断，是否继续？"
```

### 8.5 首次成功页

不要只说"完成了"，要展示具体产出 + 明确下一步：

```
52 张发票 → 3 趟行程 · 合计 ¥8,234.00
⚠️ 拦截 1 张重复（6 月已报的 G13 ¥553）

已生成：分类目录 / 报销汇总表.xlsx / 打印合订本.pdf（38 页）
       已发送 52 张到 Concur 收据库

接下来：
1. 打印合订本，按页码顺序装订
2. 打开 Concur，用汇总表填写费用条目（收据已在库中，可直接关联）
```

### 8.6 留存机制

月度使用的产品最大风险是被遗忘，一年只有 12 个触点。

**月度提醒（最重要）**：不用固定"月末"——各公司报销周期不同。**学习用户自己的节奏**（历史都在月末最后几天 → 下次提前 2 天提醒）。用邮件发送，不依赖桌面应用是否运行。

**台账价值显性化**：见 M3。

**新发票后台检测**：需常驻进程，MVP 不做，用月度邮件替代。

### 8.7 输出物不加工具标识

| 输出物 | 去向 | 标识 |
|---|---|---|
| 打印合订本 PDF | **交给公司财务** | ❌ **绝不加** |
| Excel 汇总表 | 用户自留 | ⚠️ 可选，默认关 |

打印件加标识等于向财务暴露员工使用第三方工具，对某些公司是风险。想推荐的用户自己会说。

---

## 九、观测指标

每一项都对应一个可能改变产品方向的判断。

| # | 假设 | 指标 | 阈值 | 超阈值怎么办 |
|---|---|---|---|---|
| 1 | 归组够准 | 平均调整张数/批次 | **< 3** | 看调整类型分布：集中→补规则；分散→算法回炉 |
| 2 | IMAP 可用 | 邮箱配置成功率 | > 70% | 低于此说明外企禁用普遍，文件导入提为主通道 |
| 3 | OCR 需求量 | 需手工录入的票占比 | < 20% | 超过则 v0.5 加云 OCR 兜底 |
| 4 | 定价可接受 | 第 3 次（首次付费）转化率 | > 50% | 判断是定价问题还是价值问题 |
| 5 | 方案 B 有效 | Concur 邮箱验证完成率 | > 60% | 低于此说明门槛太高，改进教程 |
| 6 | 打印有价值 | 打印件下载/打开率 | > 70% | 低于此说明多数公司已电子化，降优先级 |
| 7 | 台账有价值 | 检测到重复的用户占比 | > 15% | 低于此说明重复报销不是真痛点，订阅卖点要换 |
| 8 | 留存 | 连续 3 月回访率 | > 60% | 低于此说明被遗忘，加强提醒 |

#1 和 #4 是最关键的两项——前者决定产品能不能用，后者决定生意能不能成。

#6 可以在 β 招募时直接问，不用等数据。

---

## 十、测试计划

### 10.1 招募筛选问题

在开发前就能修正优先级：

1. 你公司用 Concur 报销吗？
2. 平均每月报销多少张发票？
3. **报销需要寄纸质发票吗？** → 直接回答观测假设 #6
4. **公司邮箱能用第三方邮件客户端（Outlook 手机版、Foxmail）吗？** → 探测 IMAP 可用性
5. 整个报销流程大概花多久？

第 3、4 题的答案分布，能在写代码前就修正模块优先级。

### 10.2 两个阶段

| | α（内测） | β（付费测试） |
|---|---|---|
| 人数 | 5 | 30–50 |
| 用户 | 最熟的人 | 种子网络 + 内容引流 |
| 数据 | 历史批次（试运行模式） | 真实当月报销 |
| 计费 | 不计费 | ¥20 赠送 + 手工充值 |
| **核心目标** | **归组准确率** | **付费转化 + 留存** |
| 反馈方式 | 一对一深聊（每人 30 分钟） | 问卷 + 关键用户访谈 |

**α 的具体做法**：5 个用户各提供 3–5 个历史月份的发票，试运行模式跑完，然后**逐批次和用户一起看归组结果**。25 个批次的样本量足以判断假设 #1。

若调整张数超过 3 张/批次，在 α 阶段迭代算法，不进 β。判断依据是调整类型分布：集中在少数几类（如全是"周末夹中间"判错）是规则问题，补规则即可；分散在各种类型才是框架问题。

---

## 十一、增长策略

### 11.1 约束：获客成本必须接近零

单用户 LTV 约 ¥300（ARPU ¥150 × 留存 2 年）。这意味着付费获客基本不可行，**内容营销 + 口碑是唯一可行渠道**，病毒机制不是可选项而是全部策略。

### 11.2 天然的传播结构：按公司渗透

**同一家公司的同事，痛点完全相同**——同一个 Concur 租户、同样的费用类型、同样的报销政策、同样的邮箱域名。

这比一般 to C 工具的传播结构好得多：不是"我觉得好用你可能也用得上"，而是"我们的问题一字不差"。

```
公司的第 1 个用户   → 完整配置（邮箱 + Concur 验证 + 费用类型映射）
公司的第 2..N 个用户 → 复用第 1 个人的配置档案
```

### 11.3 配置档案共享（本地导出/导入）

C0 探测产出的档案不含任何个人或财务信息：

```json
{
  "公司标识": "company.com",
  "concur_ui_版本": "nextgen",
  "费用类型映射": {"铁路": "TRV-RAIL-CN", "酒店": "TRV-HOTEL-CN"},
  "必填字段": ["成本中心", "项目号"],
  "expenseit_已启用": false
}
```

**选择本地导出/导入而非云端按域名匹配**，理由不只是隐私：「我把配置文件发给你」这个动作**本身就是一次背书**。云端自动匹配摩擦更低，但少了人际推荐环节——而人际推荐是我们唯一负担得起的获客方式。

MVP 极简版：导出费用类型映射表（用户手工填一次后可复制给同事）。

### 11.4 内容营销三层

| 层次 | 内容 | 目的 | 平台 |
|---|---|---|---|
| **痛点** | "Concur 报销实操：一次差旅 50 张发票怎么整理" | SEO 长尾，抓高意图搜索 | 知乎、小红书 |
| **方法** | "我用 Excel + 命名规范把报销时间砍了一半" | 建立专业感，**不提工具** | 知乎、少数派 |
| **产品** | "我做了个工具解决自己的报销问题" | 转化 | V2EX、即刻、小红书 |

第二层是关键——**先把方法免费讲透，再提工具**。可信度远高于直接推产品。

**高意图搜索词**：
- Concur 报销 教程 / 技巧 / 批量
- 出差发票 整理 / 归类
- **数电票 XML 怎么打开** ← 真实且高频的困惑，搜这个词的人一定在处理发票
- 发票 重复报销 检查
- 火车票 电子发票 报销

**节奏**：α 阶段就开始写（不等产品完成），SEO 需要时间积累。3 个月 5 篇扎实的，胜过 1 个月 15 篇水文。

### 11.5 关键时刻：第 3 次使用

¥20 覆盖约 2 个批次，而报销是月度周期：

```
第 1 月免费 → 第 2 月免费（余额归零）→ 第 3 月首次付费  ← 关键时刻
```

第 2 次完成后给累计价值回顾：

```
您已完成 2 次报销整理
累计处理  97 张发票
累计节省  约 5 小时
拦截重复  1 张（¥553 高铁票）

免费额度已用完，下次整理约需 ¥10
```

「拦截重复 1 张」比「节省 5 小时」更有说服力——它是可验证的、避免了实际损失的具体事件。

---

## 十二、风险与合规

### 12.1 合规底线

| 项 | 做法 |
|---|---|
| 不生成、不修改发票 | 只能替换（重新解析），任何编辑写审计日志 |
| 方案 C 定位 | 本地辅助填写，用户在场且可见，**最终提交由人点击** |
| 不存储公司凭证 | Concur 会话仅本地内存 |
| 责任归属 | 输出是建议，不替代财务合规判断 |
| 首次启用 C | 要求用户确认公司 IT 政策允许 |

### 12.2 主要风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| **方案 C 的条款与 IT 政策** | 最大风险，工具可能被公司禁用 | 方案 B 零风险地基；C 严格限定本地辅助；纯本地模式可用 |
| **外企 Office365 禁用 IMAP** | 主通道失效 | 文件夹/eml 导入兜底，教程讲清 |
| 各公司 Concur 配置差异 | C 无法通用 | C0 配置探测 + 用户确认一次 |
| ExpenseIt 强制启用 | 生成字段错误的条目 | 先发 1 张试探 |
| Concur UI 改版 | C 失效 | 分级降级；B 不受影响 |
| 归组准确率不达标 | 产品价值不成立 | α 阶段验证，未达标先迭代 |
| 各公司报销规则差异大 | 规则校验不准 | 规则包配置化，v1 只做通用 + 自定义 |
| 按次付费留存弱 | 用户遗忘 | 跨月台账 + 月度提醒 + 订阅 |

---

## 十三、路线图

无企业版计划，全部围绕个人版演进。

| 版本 | 内容 | 用户可感知的变化 |
|---|---|---|
| **v0.1 MVP** | 见第六章完整范围 | 150 分钟 → 15 分钟；发票已进 Concur 收据库 |
| **v0.5** | 方案 C（C0 配置探测 + C1 建报告 + C2 填条目 + C3 关联收据）<br>云 OCR 兜底（腾讯云/讯飞比价）<br>付费发票查验（命中式触发）<br>发票替换机制<br>缺票检测<br>审核界面完整键盘流<br>台账加密云备份（可选）<br>报销人字段（代报支持） | 表单也不用填了；防作废票；补票提醒 |
| **v1.0** | 归组学习（记住用户调整偏好）<br>配置档案共享完整版<br>钉钉/企微适配器（若需求频次足够） | 越用越准 |
| **v1.5+** | 手机拍照通道（优先邮件别名方案）<br>OCR 层替换为视觉模型（成本与准确率均改善） | 出差途中即时存票 |

**v0.5 的优先级顺序**由观测指标决定，不预设。例如假设 #3（手工录入占比）超 20% 则云 OCR 提前，假设 #6（打印下载率）低于 70% 则打印相关全部降级。

---

## 十四、明确不做的方向

| 方向 | 为什么不做 |
|---|---|
| **企业版** | 需要销售团队；且个人版的方案 C 恰是企业 IT 最反感的机制，不是自然升级路径 |
| 采购发票 / 供应商对账 | 用户是财务人员而非员工，完全不同的产品 |
| 个人记账 / 自由职业税务 | 场景是"抵扣"不是"报销"，无行程概念、无打印需求 |
| 差旅预订 | 不同的生意，涉及供应链 |
| **预算建议**（"这趟超标了"） | 员工要么不知道政策（公司该告知），要么知道但无控制权（提醒无用）；且我们不知道各公司标准 |
| 报销系统本身 | 卖给企业，需销售团队 |
| 汇联易/每刻/易快报适配器 | 它们自己做发票采集，我们过去只剩归组，价值主张薄 |
| 云端多设备同步 | 报销是月度集中任务，非持续协作；台账导出/导入已覆盖核心需求 |
| 自动开票（登录各平台开票） | 账号风控与安全风险高；改为产出开票缺口清单 |

**替代方案**：缺票检测（"你 7/4 在上海但那天没有任何票据"）比预算建议有价值得多——它防止退单返工，是实际损失。

### 一条保留可选性的路（不是企业版）

未来可能把**行程归组引擎 + 中国票种解析库**作为 SDK/API 授权给国内报销 SaaS（它们有企业客户关系，但归组能力大概率弱）。

| | 个人版 | 引擎授权 |
|---|---|---|
| 客户数 | 数千个人 | 1–3 家 SaaS |
| 获客 | 内容 + 口碑 | 一次 BD |
| 需要团队 | 不需要 | 不需要 |

现在不做任何事，前提是引擎真的好到能卖（需 1–2 年打磨）。当下只需守住架构纪律：**归组引擎与 UI 解耦**。

---

## 附录：竞品调查

### A.1 查到的

**「发票盒子」**（通过 GitHub 联盟营销页 `rqoz/invoicebox` 发现，2 stars，无代码）

宣称能力：邮箱/微信/支付宝采集、AI 分类去重、生成报销单 PDF、免费 100 张/年。

重叠：采集、去重、生成报销单。
**不做**：出差行程归组、对接任何企业报销系统、打印排版。

线索到此断裂——其指向的 `lizhi.shop` 是软件商店，整站无发票/报销类产品。无法验证该产品的真实状态。

**Concur App Center 的中国发票相关应用**：未搜到。也未找到 SAP 官方对中国数电票的原生支持说明。

### A.2 结论的置信度：低

多数查询返回噪音或空结果。「搜不到」不等于「不存在」，原因是结构性的：

- **微信小程序对外部搜索引擎基本不可见**，而"发票管家"类工具最可能就是小程序形态
- 中文长尾内容搜索覆盖差
- 企业级产品（汇联易、每刻、易快报、分贝通）靠销售不靠 SEO
- Concur App Center 需站内检索，外部抓不到

**判断**：没有证据表明存在直接竞品，也没有足够证据说没有。

### A.3 更可靠的验证方式（待执行）

| 动作 | 怎么做 | 看什么 |
|---|---|---|
| **问种子用户**（最强） | 招募时加问："你现在用什么工具/方法整理发票？" | 多数说"手工" → 市场空着；说出产品名 → 立刻去看 |
| 微信搜小程序 | 搜"发票""报销""发票整理" | 有哪些、做到什么程度 |
| 小红书搜 | 搜"报销 发票 整理""Concur" | 用户在讨论什么工具、抱怨什么 |
| Concur App Center 站内搜 | 登录后搜 "China" / "invoice" / "fapiao" | 有无中国发票第三方应用 |

第一个最有价值——同时验证竞品和痛点。

### A.4 市场结构推断（非搜索结论）

```
企业报销系统（汇联易/每刻/易快报/分贝通/Concur）
    ↑ 卖给企业，是"报销系统"本身

    ← 我们在这里：个人侧的预处理层
      邮箱采集 → 解析 → 行程归组 → 打印排版 → 喂给上面的系统

存票类工具（发票管家类小程序）
    ↓ 卖给个人，只做"存 + 汇总"
```

差异化集中在三点，恰好都是别人不太会做的：

1. **行程归组** —— 领域逻辑重，只对差旅人群有价值，通用工具没动力做
2. **Concur 对接** —— 用户面窄，但对这批人是刚需
3. **打印排版** —— 看起来土，但省时最多（60 分钟）

### A.5 一个技术副产品

[讯飞票据识别 API](https://www.xfyun.cn/doc/words/invoiceIdentification/API.html) 的票种覆盖比腾讯云文档更明确，且**无需指定 type**（自动判别票种）：

> 增值税专用发票、机动车销售统一发票、增值税普通发票、增值税普通发票(电子)、增值税普通发票(卷式)、二手车销售统一发票、火车票、船票、定额发票、机打发票、出租车发票、客运发票、航空运输电子客票行程单、销货清单、车辆通行费

若能自动识别票种，OCR 前的"先判票种再选模板"这一步可省。v0.5 做云 OCR 兜底时，讯飞与腾讯云一起比价和比准确率。

---

## 附录：参考资料

**Concur 邮件收单**
- [How Do I Email Receipts to SAP Concur?](https://community.concur.com/t5/Support-and-FAQs/How-Do-I-Email-Receipts-to-SAP-Concur/ba-p/16749)
- [Emailing Receipts — 支持的文件格式](https://www.utoledo.edu/offices/controller/accounts_payable/concur_training/emailingreceipts.html)
- [Working with Available Receipts — 5MB 与格式限制](https://www.ibuy.gwu.edu/sites/g/files/zaxdzs4686/files/2022-06/exp-nui-working-with-available-receipts.pdf)
- [Manage Receipts — 单次 10 文件上限](https://mylinccontent.dsc.umich.edu/mais/html/PR_TE_ExpenseReport_ManageReceipts.html)
- [Concur FAQs (Duke) — 文件名会显示在 Concur 中](https://finance.duke.edu/faq/)

**为何排除 Concur 导入方案**
- [Importing Expenses（社区提问，说明标准 Concur 无终端用户导入）](https://community.concur.com/t5/Concur-Expense/Importing-Expenses-Concur-Expense/td-p/79812)
- [Importing Expense Types — 入口在 Expense Admin](https://learning.sap.com/courses/working-with-primary-configuration-in-concur-expense-professional-edition/importing-expense-types-2)
- [Importing Attendees — 需 Configuration Administrator 角色](https://learning.sap.com/courses/getting-started-with-concur-expense-professional-edition-for-administrators/importing-attendees_f6f9bd65-b5ec-4247-961f-c40f3426edd2)

**模型与成本**
- [DeepSeek V4 API Pricing](https://apidog.com/blog/deepseek-v4-api-pricing/)
- [DeepSeek-V4-Flash-0731 定价](https://explainx.ai/blog/deepseek-v4-flash-0731-codex-responses-api-july-2026)
- [DeepSeek API 是否支持图像输入](https://chat-deep.ai/docs/deepseek-api-vision-input/)（确认无原生视觉）

**发票解析**
- [ofdparser：OFD 验签与信息提取](https://github.com/gongdaowen/ofdparser)
- [invoice-ofd2json：解析 OFD 为 JSON](https://github.com/DreamNya/invoice-ofd2json)
