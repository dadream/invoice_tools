> **历史参考，不作为当前范围依据。** 当前任务计划见 `specs/mvp-release-baseline/tasks.md` 1.0。
> 本文的阶段、路径、安全和完成度描述可能已过期。

# 发票报销 AI 助手 - 开发计划

> **依据**：`invoice-reimbursement-product-spec.md`（产品方案）、`invoice-reimbursement-pricing.md`（计费方案）
> **组织方式**：按**依赖顺序**编排，不含时间估算
> **范围**：MVP（v0.1）全部内容
> **最后更新**：2026-08-03

---

## 目录

1. [技术栈决策：纯 Rust vs Python Sidecar](#一技术栈决策纯-rust-vs-python-sidecar)
2. [依赖结构总览](#二依赖结构总览)
3. [L0 地基层](#三l0-地基层)
4. [L1 六条并行轨道](#四l1-六条并行轨道)
5. [L2 依赖层](#五l2-依赖层)
6. [L3 集成层](#六l3-集成层)
7. [L4 发布前](#七l4-发布前)
8. [外部阻塞项](#八外部阻塞项)
9. [测试策略](#九测试策略)
10. [MVP 完成定义](#十mvp-完成定义)

---

## 一、技术栈决策：纯 Rust vs Python Sidecar

### 1.1 核心冲突

产品方案定的技术栈里，解析与 OCR 全部是 Python 生态：

| 能力 | 原计划 | 语言 |
|---|---|---|
| 数电票 XML 解析 | lxml | Python |
| OFD 解析 | ofdparser | Python/Java |
| PDF 文本层 | pdfplumber | Python |
| 本地 OCR | PaddleOCR | Python |

而 Tauri 的后端是 Rust。两条路：

| 方案 | 包体积 | 后果 |
|---|---|---|
| Python Sidecar | **300MB+**（Python 运行时 + PaddlePaddle + 模型） | **Tauri 的核心优势消失** |
| 纯 Rust | 10–30MB | 保持 Tauri 优势，但需验证 crate 成熟度 |

**这个决策是承重的**：选 Tauri 的理由就是"10MB vs 120MB 是首次下载的第一印象"。如果最终包体 300MB+，那当初该选 Electron（生态更成熟、Python 集成更容易）。所以这不是一个实现细节，而是可能推翻架构决策的问题。

### 1.2 调研结果：四项能力都有 Rust 方案

| 能力 | Rust 方案 | 备注 |
|---|---|---|
| **本地 OCR** | [paddle-ocr-rs](https://github.com/mg-chao/paddle-ocr-rs)（ONNX Runtime 跑 PaddleOCR 模型）<br>[ocr-rs](https://lib.rs/crates/ocr-rs)（PaddleOCR 模型 + MNN 推理） | 用的是同一批 PaddleOCR 模型，识别能力等价 |
| **SM2/SM3 验签** | [smcrypto](https://github.com/zhuobie/smcrypto)、[gm-rs](https://github.com/CrayfishGo/gm-rs) | 国密算法纯 Rust 实现 |
| **PDF 文本层** | [pdf-extract](https://github.com/jrmuizel/pdf-extract)、[pdfplumber-rs](https://github.com/developer0hye/pdfplumber-rs) | 后者是 pdfplumber 的 Rust 移植，带坐标定位 |
| **OFD 解析** | **自行实现**（`zip` + `quick-xml`） | OFD 是 ZIP+XML 结构（GB/T 33190-2016），参考 [ofdparser](https://github.com/gongdaowen/ofdparser)(Java)、[invoice-ofd2json](https://github.com/DreamNya/invoice-ofd2json)(JS) 的字段定位逻辑 |

OFD 值得单说：它本质是个 ZIP 包，内含 `OFD.xml`、`Documents/`、以及数电票的内嵌 XML。解析不需要渲染整个版式文档——**只需要定位并读出内嵌的发票 XML**，这是个明确有界的任务，不依赖任何重型库。

### 1.3 决策：纯 Rust 优先，Sidecar 兜底

**默认路线**：纯 Rust，包体控制在 30MB 内（含 ONNX 模型约 10–15MB）。

**兜底条件**：若 S0.1 验证失败（某项能力的 Rust crate 无法处理中国发票的实际样本），则：

```
优先级 1  该项改用 Rust FFI 调 C/C++ 库（如 ONNX Runtime C API）
优先级 2  仅该项走 Python sidecar，其余保持 Rust
优先级 3  全部走 Python sidecar → 此时应重新评估 Tauri vs Electron
```

**重要**：这些 Rust crate 中有相当一部分很新，未经中国发票场景的大规模验证。成熟度必须用真实样本验证，不能只看 README——这正是 S0.1 的目的。

---

## 二、依赖结构总览

```
┌─ L0 地基层（阻塞一切）─────────────────────────────┐
│ S0.1 技术验证    S0.2 Tauri 骨架   S0.3 核心数据模型 │
│ S0.4 加密存储    S0.5 批次状态机   S0.6 错误分类     │
└────────────────────┬───────────────────────────────┘
                     ↓
┌─ L1 六条并行轨道（互不依赖）───────────────────────┐
│ A 采集    B 解析    C 归组引擎                      │
│ D 输出    E 计费    F 适配器                        │
└──────┬──────────┬──────────────────────────────────┘
       ↓          ↓
┌─ L2 依赖层 ────────────────────────────────────────┐
│ G1 校验去重 ← B        G2 审核界面 ← B + C          │
└────────────────────┬───────────────────────────────┘
                     ↓
┌─ L3 集成层 ────────────────────────────────────────┐
│ H1 流水线串联  H2 设置  H3 首次运行  H4 首次成功页  │
│ H5 台账导出/导入  H6 月度提醒  H7 匿名遥测          │
└────────────────────┬───────────────────────────────┘
                     ↓
┌─ L4 发布前 ────────────────────────────────────────┐
│ I1 跨平台构建  I2 合规文档  I3 α 测试工装           │
└────────────────────────────────────────────────────┘
```

**关键路径**：`S0.1 → S0.3 → B（解析）→ G1/G2 → H1`

**归组引擎（C）可以完全独立开发**——它的输入是发票列表，用合成数据就能测。这是最大的并行机会，也是风险最高的模块（α 验收线卡在这里），应尽早启动。

---

## 三、L0 地基层

这一层的产出决定后面所有模块的形状，**必须一次做对**（产品方案 §技术债务清单）。

### S0.1 技术验证（第一个任务，阻塞架构决策）

用真实发票样本逐项验证 Rust 方案。每项的验收标准是"能处理我手上的真实样本"，不是"crate 能编译"。

| 验证项 | 输入 | 通过标准 |
|---|---|---|
| 数电票 XML 解析 | 5 张不同来源的数电票 XML | 金额、税额、税率、发票号、日期、买卖方全部正确读出 |
| OFD 解析 | 5 张 OFD 发票（含专票/普票） | 能定位并读出内嵌 XML，字段同上 |
| SM2/SM3 验签 | 上述 10 张 | 验签结果与官方查验一致（至少 1 张已知作废票做负例） |
| PDF 文本层 | 铁路行程单 3 张 + 航空行程单 3 张 + PDF 版增值税票 3 张 | 车次/航班号、金额、日期正确；表格结构不错行 |
| **本地 OCR** | **10 张图片格式增值税票**（扫描件 + 手机照片各半） | **金额与税额准确率 ≥ 90%，且置信度能区分对错** |
| ONNX 模型打包 | 打进 Tauri 应用 | 三平台均能加载推理，安装包 < 30MB |

**OCR 那一项是最大的不确定性**：Rust 侧跑的是 PaddleOCR 的 ONNX 导出模型，识别能力理论上等价，但预处理（倾斜校正、二值化）和后处理（字段定位）需要自己实现，这部分是 PaddleOCR Python 版的 `PPStructure` 提供的能力。**如果字段定位效果差，这一项最可能触发 sidecar 兜底。**

**产出**：一份验证报告，明确每项通过/不通过，以及最终技术栈决定。

### S0.2 Tauri 骨架 + 构建流水线

- Tauri v2 项目结构，前端框架自选（UI 不复杂，无强制要求）
- 三平台（Windows / macOS / Linux）本地构建打通
- IPC 通道约定：前端调用 Rust 命令的错误传递格式
- 日志：写本地文件，不上传

**验收**：三平台各产出一个能启动的空壳应用。

### S0.3 核心数据模型（一次做对）

**纪律：不允许出现任何 Concur 概念**（产品方案 §4.3 纪律 3）。

```rust
struct Invoice {
    id: InvoiceId,
    票种: TicketType,           // 铁路 | 航空 | 酒店 | 市内交通 | 餐饮 | 其他
    日期: NaiveDate,
    金额: Decimal,              // 用 Decimal，不用 f64
    税额: Option<Decimal>,
    税率: Option<Decimal>,
    发票号: String,
    买方: String, 卖方: String,
    城市: Option<String>,
    行程归属: Option<TripId>,
    报销人: String,             // 代报场景的隔离键
    解析级别: ParseLevel,       // L0 | L1 | L2 | L4
    置信度: f32,
    原件路径: PathBuf,
    // ❌ 不出现 expense_type / report_header / itemization
}

struct Trip {
    id: TripId,
    kind: TripKind,             // 出差 { start, end, city_chain } | 市内 { 年月 } | 待确认 | 排除
    invoices: Vec<InvoiceId>,
}

struct Batch {
    id: BatchId,
    状态: BatchState,           // 见 S0.5
    发票: Vec<InvoiceId>,
    行程: Vec<Trip>,
    是否试运行: bool,           // 试运行不计费、不写台账
}
```

**金额必须用 Decimal**（如 `rust_decimal`），浮点会在求和对账时产生分位误差——而金额对账是防"静默的错"的核心防线（产品方案 §M5）。

**验收**：模型定义 + 单元测试覆盖序列化/反序列化；代码审查确认无 Concur 词汇泄漏。

### S0.4 加密存储

- SQLite 双库：`accounts.db`（账号/余额/凭证/设置）、`ledger.db`（发票/批次/台账）
- 邮箱凭证 AES-256-GCM 加密
- **密钥从系统 Keychain 派生**：macOS Keychain / Windows DPAPI / Linux Secret Service
- 表结构见产品方案 §4.4

**验收**：凭证写入后，直接打开 SQLite 文件无法读出明文；应用重装后仍能解密（密钥在系统 Keychain 里）。

### S0.5 批次状态机 + 持久化

```
采集完成 → 解析完成 → 校验完成 → 归组完成 → 已输出
```

每步完成写库。重启时检测未完成批次，提示"上次的批次在【X】步骤中断，是否继续？"

**验收**：在每个状态下强杀进程，重启后都能正确恢复并继续。50 张跑到第 40 张时中断，不能从头再来。

### S0.6 错误分类与呈现约定

三类错误（产品方案 §8.3），在地基层就定好类型和呈现契约：

| 类型 | Rust 类型 | 前端呈现 |
|---|---|---|
| 用户可修复 | `UserFixable { what, how, jump_to }` | 明确说改哪里 + 一键跳转 |
| 需要决策 | `NeedsDecision { .. }` | 进审核界面，**不叫"错误"** |
| 系统故障 | `SystemFault { retriable }` | 说明 + 保留进度 + 重试 |

**同时确立措辞规则**：技术分级（L0/L1/L2、置信度）不出现在任何用户可见文案里。

**验收**：所有错误路径都归到三类之一；有一份文案对照表（技术表述 → 用户表述）。

---

## 四、L1 六条并行轨道

六条轨道只依赖 L0，彼此独立。

### 轨道 A：采集（M1）

| ID | 任务 | 验收 |
|---|---|---|
| A1 | IMAP 客户端，App 专用密码认证 | 能连 Gmail / QQ / 163 / Outlook 四种，拉取指定时间范围的邮件 |
| A2 | 三级漏斗第 1 级：发件人白名单 | 内置默认列表 + **启动时拉取云端 JSON 更新**（避免加平台就发版） |
| A3 | 三级漏斗第 2 级：附件特征 | `.xml`/`.ofd`/`.pdf` 且文件名或首页含"发票""行程单""结算单" |
| A4 | 三级漏斗第 3 级：LLM 判定 | 仅剩余歧义邮件（5–10 封/批次），输入发件人+主题+附件名+正文前 200 字 |
| A5 | 幂等去重三重键 | `Message-ID` + 文件 SHA256 + 发票号。同一张票平台重发 + 用户转发，收敛为一条 |
| A6 | 兜底通道：`.eml` / 文件夹 / 图片拖入 | 与 IMAP 走同一后续流程 |
| A7 | 各邮箱配置图文教程 | 重点讲清"应用专用密码 ≠ 登录密码" |

**已知风险**（产品方案 §M1）：外企 Office365 可能禁用 IMAP。A6 和 A7 不是可选项——A7 的教程质量直接影响观测指标 #2（邮箱配置成功率 > 70%）。

### 轨道 B：解析（M2）——关键路径

| ID | 任务 | 依赖 | 验收 |
|---|---|---|---|
| B1 | 格式路由器 | S0.3 | 按扩展名 + 文件头判定走 L0/L1/L2 哪条路 |
| B2 | L0：数电票 XML | S0.1 | 字段准确率 100%（结构化数据无歧义） |
| B3 | L0：OFD（ZIP+XML 定位内嵌发票 XML） | S0.1 | 同上；能处理专票/普票/全电票 |
| B4 | L1：PDF 文本层 + 票种版式模板 | S0.1 | 铁路行程单、航空行程单、PDF 增值税票，准确率 > 99% |
| B5 | **L2：本地 OCR + 增值税票字段提取** | S0.1 | 金额/税额/税率/发票号/日期，准确率 92–95% |
| B6 | 置信度评分 + L4 升级（关键字段冲突 → 强制人工） | B2–B5 | 置信度能有效区分对错（低分的确实更容易错） |

**B5 是 MVP 里技术风险最高的单点**。它必做的理由是税率/税额是 Concur 必填项，而图片格式的增值税票（定额发票、餐饮/酒店纸票扫描件）只有图，占差旅批次约 15–25%。

**架构纪律**：票种模板集中管理在一处，不散落。为未来"图片 → 视觉模型 → 结构化字段"的替换留出口（产品方案 §4.3 纪律 2）。

### 轨道 C：归组引擎（M4）——最高风险，可完全独立开发

**纪律：与 UI 解耦**，输入 `(Vec<Invoice>, 常驻城市列表, 配置)` → 输出 `(Vec<Trip>, 置信度, 歧义列表)`（产品方案 §4.3 纪律 1）。

| ID | 任务 | 验收 |
|---|---|---|
| C1 | 确定性算法 7 步 | 见下 |
| C2 | 歧义检测（5 类） | 能正确识别出需要 LLM 的场景，不误报 |
| C3 | LLM 批量判定 | **一次调用**处理本批次全部歧义；输入 ~2K tokens，成本 ~¥0.01 |
| C4 | 置信度输出 + 边缘情况处理 | 低置信项进"待确认"桶 |

**C1 必须确定性解决的场景**（不调 LLM，直接靠规则）：

- 同日多城（`city_chain` 累加）
- 跨国/跨地区（香港、新加坡、台北按地区处理）
- **火车中转 < 4h**（判中转，不是行程点）
- **去机场/车站的市内票**（时间窗 `[start−6h, end+6h]`）
- **酒店延迟开票**（用 `checkin_date` 而非 `invoice_date`）
- 纯市内出差（无城际票 → 直接进市内桶）

把这些写死是把确定性覆盖率从 70% 推到 80% 的关键——这是达到 α 验收线（< 3 张/批次）的必要条件。

**C2 必须交给 LLM 的 5 类**：无返程票且下趟起点不同、周末夹中间无明确返程、中转停留 4–12h、同城短期多次往返、时间重叠。

**开发方式**：先用**合成数据**建测试集（手写 20–30 个场景的发票列表 + 期望分组），不等 B 轨道完成。这样归组的正确性可以独立验证。

| ID | 任务 | 验收 |
|---|---|---|
| C5 | 合成测试集 | 覆盖上述所有确定性场景 + 5 类歧义，每个场景有期望输出 |
| C6 | 真实数据回归 | α 阶段用真实历史批次跑，**平均调整 < 3 张/批次** |

**已知限制**（不在 MVP 解决，文档明示）：多人同行票无法识别（需用户拖入"排除"桶）。

### 轨道 D：输出（M6）

| ID | 任务 | 验收 |
|---|---|---|
| D1 | 标准目录结构 + 文件重命名 | 命名格式 `{日期}_{类型}_{摘要}_{金额}_{票种}.{ext}`；`99_原始件/` 永不覆盖 |
| D2 | Excel 汇总表 | **含"Concur 费用类型"映射列**（用户填一次可复用/给同事） |
| D3 | **发票渲染为 PDF**（XML/OFD → PDF） | D3 和 F3 共用；渲染结果人眼可读、字段完整 |
| D4 | A4 打印合订本 | 行程单独占页；小票 6 张/页带序号；页码 + 首页目录 |
| D5 | Concur 录入清单 CSV | 字段与 Excel 一致 |
| D6 | 打印件页数校验 | 生成后校验页数与预期一致（防漏页） |

**D4 做够用就停**（产品方案 §2.2）：不做智能排版、不做按票据尺寸自适应。观测指标 #6（打印件下载率）低于 70% 即降优先级。

**D3 是共享依赖**：打印合订本需要它，方案 B 发邮件也需要它（Concur 不认 XML/OFD）。应优先于 D4 和 F3。

### 轨道 E：账号与计费

| ID | 任务 | 验收 |
|---|---|---|
| E1 | 账号注册/登录 | 邮箱 + 密码；云端只存账号和余额 |
| E2 | 计费引擎 | `实收 = max(¥3, min(张数 × ¥0.20, 报销总额 × 0.002))`，用 Decimal |
| E3 | 余额扣费 + 流水记录 | 扣费失败不能导致批次数据丢失 |
| E4 | ¥20 注册赠送 | 新账号自动到账 |
| E5 | **手工充值后台** | 个人收款码 + 管理员手工加余额（商户号正式上线前的桥接） |
| E6 | **试运行模式** | 跑完整流程但**不计费、不写台账**，输出加 `[试运行]` 前缀 |
| E7 | 累计价值回顾 | 第 2 次完成后展示"处理 N 张 / 节省 N 小时 / **拦截重复 N 张**" |
| E8 | 费用预估展示 | 只显示最终价，计算详情折叠 |

**E2 的公式方向必须反复确认**：先 `min`（两种计费取对用户便宜的），再 `max`（¥3 保底）。方向搞反会导致收费翻倍。建议写成表驱动测试，把产品方案 §7 的速查表全部作为测试用例。

**E6 试运行模式不是可选项**：它是"敢不敢用"到"愿不愿付"之间的桥，也是 α 阶段用真实历史数据测试的前提。

### 轨道 F：适配器层

| ID | 任务 | 验收 |
|---|---|---|
| F1 | 适配器接口定义 | 见产品方案 §4.3；`capabilities.提交` 永远 `false` |
| F2 | **GenericAdapter** | 全部 capability 为 false，只输出 Excel/CSV + 打印件（纯本地模式） |
| F3 | ConcurAdapter：邮件收单 | 见下 |

**F2 几乎零成本但必须做**——它是验证抽象未被 Concur 污染的唯一方式。只实现一个适配器，接口最后一定长成 Concur 的形状。

**F3 子任务**：

| ID | 任务 | 验收 |
|---|---|---|
| F3.1 | 结构化文件名 | `{日期}_{类型}_{金额}_{发票号后6位}.pdf`，**不含批次序号**（全局库会撞号） |
| F3.2 | 图片压缩至 < 5MB | 黑白、≤1024×768 |
| F3.3 | SMTP 分批发送（每封 ≤ 10 附件） | 50 张拆 5 封，带重试队列 |
| F3.4 | **首次先发 1 张试探** | 用户确认只进 Available Receipts、无 ExpenseIt 自动建条目后，才批量发 |
| F3.5 | Concur 邮箱验证引导 | 3 张截图的图文教程（方案 B 的唯一前置条件） |

**F3.4 的必要性**：部分公司在租户级强制启用 ExpenseIt，它会自动 OCR 建条目——而它对中国发票不可靠，会生成一堆字段错误的条目，第一次使用就搞乱用户的 Concur。

**每张发票独立文件，绝不发合订本**——Concur 每个费用条目要关联自己的收据。

---

## 五、L2 依赖层

### G1 校验与去重（M3）← 依赖 B

全部确定性，零 LLM 成本。

| ID | 任务 | 验收 |
|---|---|---|
| G1.1 | 结构校验 | 发票号格式、日期合法性、税率合规、**价税合计恒等式** |
| G1.2 | 本地验签（SM2/SM3） | 数电票 XML/OFD 签章验证；已知作废票能被识别 |
| G1.3 | 本批去重 | 发票号为主键 |
| G1.4 | **跨月台账** | 独立表 `reported_invoices`；按 `报销人` 隔离（代报场景） |
| G1.5 | 规则校验 | 抬头、税号、开票日期超期（规则包可配置） |
| G1.6 | 台账价值显性化 | 即使未检出重复也展示"已对照 N 个月历史台账（N 张），未发现重复" |

**G1.4 是订阅的核心价值，也是长期资产**（产品方案 §2.2）。表结构必须一次做对——它是用户数据，迁移风险高。

**G1.6 不能省**：跨月去重默认是隐形的，没检出重复时用户感受不到这个检查发生了。观测指标 #7（检出重复的用户占比 > 15%）依赖这个功能被用户看见。

### G2 审核界面（M5）← 依赖 B + C

MVP 用简化版，但有三项不能简化。

| ID | 任务 | 验收 |
|---|---|---|
| G2.1 | 单屏布局：左行程树 + 右发票列表 | 异常项红标；点击行程过滤右侧 |
| G2.2 | 调整操作：拖拽 / 批量移动 / 拆分 / 合并 / 排除 | 5 张调整能在 2 分钟内完成 |
| G2.3 | **紧凑纸票录入界面** | 图在左，金额/税额/日期三输入框在右，Tab 切换，Enter 提交并跳下一张 |
| G2.4 | **金额合计强制对账** | 显示总额 + 各行程分项 |
| G2.5 | **OCR 结果显示原图对照** | 提取值旁边放原图缩略图 |
| G2.6 | 四种打断条件 | 低置信度 / 规则冲突 / 单张超阈值（默认 ¥2000）/ 归组歧义 |

**G2.3–G2.5 是防"静默的错"的三道防线**（产品方案 §M5）。比"报错"更糟的是"看起来正常但值是错的"——OCR 把 ¥128.00 读成 ¥12.80 且置信度 0.85，后果是提交后被财务退单，代价远高于我们省下的时间。

**G2.3 容易被低估**：纸票录入占审核总时间的 40%（5 张 × 20 秒 = 100 秒，占 4.2 分钟里的 100 秒）。必须做成紧凑的连续录入流，不能是弹窗式逐个编辑。

**自动通过率目标 ≥ 85%**——低于此用户会觉得"还不如自己弄"。

**观察项**：如果用户频繁使用"拆分/合并"，说明 C1 的切分逻辑有问题。这是 α 阶段的重点观察指标。

---

## 六、L3 集成层

| ID | 任务 | 验收 |
|---|---|---|
| H1 | 流水线串联（采集→解析→校验→归组→审核→输出） | 端到端跑通 50 张批次；每步状态正确落库 |
| H2 | 设置界面 | 常驻城市（**用户直接填写，支持多个**）、抬头/税号、模式选择（纯本地/Concur）、遥测开关（**默认关**） |
| H3 | 首次运行引导 | 注册 → 设置 → 邮箱配置 → Concur 邮箱验证（可跳过）→ 第一个批次 |
| H4 | 首次成功页 | 展示具体产出 + **明确"接下来做什么"** |
| H5 | 台账导出/导入 | 只同步 `reported_invoices`，不含批次文件 |
| H6 | 月度提醒邮件 | **学习用户自己的节奏**（历史都在月末最后几天 → 提前 2 天发），不用固定月末 |
| H7 | 匿名遥测 | 默认关；只发结构不发内容（张数、票种分布、解析层级、调整次数、耗时） |

**H4 在 MVP 尤其重要**：没有方案 C，用户还要自己填 Concur 表单。如果不明确告知，他会以为工具没做完事。

**H7 的红线**：绝不发送金额、日期、地名、发票号、买卖方名称。

**H2 的一个细节**：输出物默认不加工具标识，打印件**永不加**（产品方案 §8.7）——打印件要交给公司财务，加标识等于暴露员工使用第三方工具。

---

## 七、L4 发布前

| ID | 任务 | 验收 |
|---|---|---|
| I1 | 三平台构建与测试 | Windows WebView2 / macOS WebKit 渲染一致性；安装包体积 < 30MB |
| I2 | 合规文档 | 隐私政策、服务条款、发票说明 |
| I3 | α 测试工装 | 能批量导入历史批次跑试运行；能导出归组调整统计 |

**I2 隐私政策必须列明的第三方**：DeepSeek API（发送压缩后的行程事件，不含金额）、支付渠道。MVP 阶段无云 OCR，所以不涉及腾讯云/讯飞。

**I2 服务条款要写清责任边界**：输出是建议，不替代财务合规判断。

**I3 是 α 阶段的效率关键**：5 个用户 × 3–5 个历史批次 = 25 个批次，需要能快速导入和统计，否则 α 会拖很久。

---

## 八、外部阻塞项

这些不是开发任务，但会卡住开发。**建议在 S0.1 之前或同时启动**。

| # | 阻塞项 | 卡住什么 | 怎么解 |
|---|---|---|---|
| 1 | **真实发票样本** | **B 轨道全部 + S0.1** | 收集自己和同事的历史发票，覆盖：数电票 XML、OFD、PDF 增值税票、铁路行程单、航空行程单、图片格式增值税票、定额发票。**每种至少 5 张** |
| 2 | **Concur 测试账号** | F3 全部（尤其 F3.4 ExpenseIt 试探） | 用自己公司的账号；需先验证邮箱 |
| 3 | 已作废/红冲发票样本 | G1.2 的负例测试 | 较难获得，可先跳过负例，标注为已知覆盖缺口 |
| 4 | DeepSeek API Key | A4、C3 | 直接申请，无门槛 |
| 5 | 主体 + 商户号 | 正式上线的支付（**不卡 MVP**） | 现在启动申请（周期以周计），MVP 用手工充值桥接 |
| 6 | α 测试用户（5 人） | I3 之后的验证 | 从种子网络招募，用 §十.2 的筛选问题 |

**#1 是最硬的阻塞**：没有真实样本，S0.1 无法验证，B 轨道无法开发，也无法判断 Rust 方案是否可行。这应该是整个项目的第一个动作——**先收集样本，再写代码**。

**#3 的处理**：作废票样本难获得，但 G1.2 的价值恰恰在于识别作废票。如果拿不到负例，就把"付费查验"从 v0.5 提前的判断依据记为"未验证"，并在文档里标注这个覆盖缺口。

---

## 九、测试策略

### 9.1 按模块的测试方式

| 模块 | 测试方式 | 关键点 |
|---|---|---|
| B 解析 | **真实样本回归集** | 每种票种 × 每种格式各 5 张，字段逐个断言。新增票种版式就加样本 |
| C 归组 | **合成场景测试集**（C5）+ 真实回归（C6） | 合成集覆盖所有确定性场景 + 5 类歧义；真实集验收线 < 3 张/批次 |
| E 计费 | **表驱动测试** | 把产品方案 §7 速查表全部作为用例；重点防 min/max 方向搞反 |
| G1 校验 | 正例 + 负例 | 作废票负例若拿不到，标注为覆盖缺口 |
| S0.5 状态机 | **故障注入** | 在每个状态强杀进程，验证恢复 |
| F3 邮件 | Concur 真实账号 | 尤其 F3.4：需要一个启用了 ExpenseIt 的租户才能测全（可能拿不到，标注） |

### 9.2 端到端测试用例

至少覆盖这几个批次形态：

| 用例 | 构造 | 验证 |
|---|---|---|
| 标准批次 | 50 张，3 趟行程，有去有回 | 全流程通过，归组零调整 |
| 无返程票 | 缺 1 张返程 | 触发 LLM 歧义判定 |
| 跨月重复 | 含 1 张上月已报的票 | G1.4 拦截并提示 |
| 全图片批次 | 20 张全是照片 | L2 全部走 OCR，需手工录入的比例可统计 |
| 纯市内 | 无城际票，30 张市内消费 | 全部进市内桶，不误判为出差 |
| 中断恢复 | 跑到归组时强杀 | 重启后能续跑 |
| 试运行 | 用历史已报批次 | 不计费、不写台账 |

### 9.3 不做的测试

MVP 阶段明确不投入的：

- 性能压测（50 张是典型规模，不需要压 500 张）
- 并发（单用户桌面应用，无并发场景）
- UI 自动化（界面还会大改，投入产出比低）

---

## 十、MVP 完成定义

### 10.1 功能完成清单

| 模块 | 完成标准 |
|---|---|
| 采集 | IMAP 四种邮箱可用 + 文件夹/eml 兜底 + 三重去重 |
| 解析 | L0/L1/L2 全部可用；L4 能正确升级到人工 |
| 校验去重 | 本地验签 + 跨月台账 + 规则校验 + 台账显性化 |
| 归组 | 确定性算法 + LLM 歧义；合成测试集全绿 |
| 审核 | 单屏 + 调整操作 + 紧凑纸票录入 + 三道防错 |
| 输出 | 目录 + Excel（含映射列）+ 打印合订本 + CSV + PDF 渲染 |
| Concur | 邮件收单全流程（含试探发送 + 验证引导） |
| 适配器 | Concur + Generic 两个；纯本地模式可用 |
| 计费 | 公式正确 + ¥20 赠送 + 手工充值 + 试运行 + 累计回顾 |
| 基础 | 状态机断点续跑 + 加密存储 + 台账导出导入 + 错误三分类 |

### 10.2 质量门槛

| 指标 | 门槛 | 来源 |
|---|---|---|
| **归组平均调整** | **< 3 张/批次** | α 验收线（话题 4 决定） |
| 自动通过率 | ≥ 85% | 产品方案 §M5 |
| L0/L1 字段准确率 | > 99% | 产品方案 §M2 |
| L2 OCR 准确率 | 92–95% | 产品方案 §M2 |
| 审核耗时 | ≤ 5 分钟 | 产品方案 §M5 |
| 安装包体积 | < 30MB | 保持 Tauri 优势 |
| 端到端耗时 | 50 张批次 < 5 分钟（不含人工） | — |

**归组那一项不达标时的处理顺序**：先看调整类型分布——集中在少数几类（如全是"周末夹中间"判错）是规则问题，补 C1 的规则即可；分散在各种类型才是算法框架问题，需要回炉 C1/C2 的设计。

### 10.3 α 阶段准备就绪的标志

- I3 工装可批量导入历史批次跑试运行
- 5 个 α 用户已招募，且已通过筛选问题确认（用 Concur、月均 ≥15 张）
- 能导出归组调整统计（调整张数 + 调整类型分布）

**α 的唯一目的是验证归组准确率**——这是 MVP 最大的技术不确定性，越早撞越好。如果 < 3 张达不到且两次迭代无改善，需要重新评估产品价值主张（这不是停止条件，而是要求重新设计归组方案）。

### 10.4 β 阶段准备就绪的标志

- α 的归组验收线已达标
- 手工充值流程可用（个人收款码 + 后台加余额）
- 手工开票流程可用
- 隐私政策 / 服务条款已上线
- 30–50 个 β 用户已招募

---

## 附：与产品方案的对应关系

| 本文档 | 产品方案章节 |
|---|---|
| §一 技术栈决策 | §4.1 Tauri + 本地优先、§4.2 技术选型 |
| §三 L0 地基层 | §4.3 三条架构纪律、§4.4 数据与隐私、§8.3/8.4 |
| §四 L1 轨道 A–F | §M1–M7、§4.3 适配器 |
| §五 L2 | §M3、§M5 |
| §六 L3 | §八 用户体验 |
| §八 外部阻塞项 | — （本文档新增） |
| §十 完成定义 | §六 MVP 范围、§九 观测指标 |

**本文档不重复产品方案的论证**，只编排实现顺序和验收标准。功能的"为什么"见产品方案。
