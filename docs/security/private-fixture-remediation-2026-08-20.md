# 历史票据夹具隐私处置记录（2026-08-20）

> 发布状态：P0 阻断。当前工作树已隔离，但删除尚未提交，当前 `HEAD`/本地 `origin/main` 跟踪引用仍包含旧夹具；Git 历史必须在任何公开仓库或公开发布前完成清理和复核。

## 发现

发布审计发现 `fixtures/test-images/` 下 20 个已跟踪历史文件，以及旧 `fixtures/manifest.toml`。不输出内容的特征检查显示：10 份 OCR JSON 均包含长数字标识符和金额；其中 6 份含发票词汇；全部缺少“合成/示例/测试”标记。提交历史说明这些文件来自历史解析样本工作，不能证明为安全的合成数据。

## 已执行止血

1. 当前副本移动到 `.private-fixtures-quarantine/legacy-invoice-fixtures-2026-08-20/`，该目录被 Git 忽略；原始文件仍可从隔离目录和 Git 历史恢复。
2. 当前 `fixtures/manifest.toml` 替换为明确标注 `synthetic-only` 的最小清单。
3. 新增 XML、EML、图片、扫描 PDF 与业务 DTO 的机器可读哈希清单。
4. 新增 `scan-private-fixtures.ps1`，并纳入统一 Windows 门禁。

## 2026-08-24 本地 Git 审计

- `HEAD` 与本地 `origin/main` 跟踪引用均为 `0f1bd561…e26c`，各自仍可达 `fixtures/test-images/` 下 20 个文件，共 926,509 B、18 个唯一 blob。
- 工作树已准备 20 个删除，旧目录已不存在，21 个隔离文件共 960,049 B 留在被忽略的 quarantine 中；这些删除尚未形成提交，因此不能声称当前分支或远端已经清理。
- 相关路径跨 13 个可达提交、70 个可达对象；本地 Git pack 仍保留对象。5 个本地产物 ZIP 的禁止条目扫描为 0。
- GitHub HTTPS 实时查询受当前网络阻断，SSH 实时查询又因本机无可用公钥认证失败；本地远端跟踪引用不能替代实时远端盘点。
- 机器证据为 `artifacts/git-privacy-history-audit.validation.json`。它只记录路径级计数、对象哈希与聚合大小，不保存或输出票据内容。

## 公开发布前必须完成

1. 先把当前工作树的删除、合成替换、扫描门禁和相关代码整理到指定候选提交；不得把 quarantine 或真实样本加入提交。
2. 确认 GitHub 实时默认分支、所有分支/标签、fork、镜像、Actions 缓存和历史 Release 是否包含旧对象。
3. 取得仓库管理员批准后，用受控历史重写或新建干净仓库移除对应 Git 对象；不得在聊天、工单或普通构建日志复制其内容。
4. 强制更新受影响远端引用，使所有协作者重新克隆，并作废旧构建缓存和旧发布包。
5. 在独立空目录做干净克隆，确认旧路径/对象不可达，再运行秘密扫描、私有夹具扫描和完整 Windows 门禁。
6. 产品负责人书面确认 P0 关闭后，才能创建公开 Beta 或正式 Release。

本记录不包含 OCR 文本、公司名、票号、税号、金额或图像预览。
