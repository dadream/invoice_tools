# Windows MVP 实施验证记录（更新至 2026-08-24）

> 状态：本地自动化、当前候选真实 QQ 只读 packaged UI 全流水线、动态隐私日志与严格空闲内存基线均通过；参考机真实 50/105 文件性能、峰值内存和冻结样本 252 个核心字段真值已完成；最低配置、完整缩放/故障/审核输出矩阵与第二台物理机未完成；不代表公开发布 Go

## 候选环境

- Windows 11 x64
- Node.js 24.14.0 / npm 11.9.0
- Rust/Cargo 1.97.1（MSVC）
- Windows SDK 10.0.26100.0
- WebView2 Evergreen 151.0.4129.93
- 产品版本 0.1.0，内部 Alpha，未签名

## 自动化门禁

统一命令：`scripts/verify-windows.ps1`

| 检查 | 结果 |
|---|---|
| 秘密与隐私扫描 | 通过；秘密扫描未发现候选文件，模拟泄漏值被门禁拒绝并清除；14 个合成夹具完成路径、标记、大小和 SHA-256 锁定 |
| 第三方许可证与资产扫描 | 通过；Cargo 701、npm 191、OFL 字体 2 组/5 个资产、OCR 8 个资产，固定版本与 SHA-256 一致 |
| Rust 格式 | 通过 |
| Clippy | 通过；workspace/all-targets/locked，`-D warnings` |
| Rust 检查 | 通过；workspace/all-targets/locked |
| Rust 测试 | 519 通过、0 失败、15 默认忽略；3 项 OCR 合成/进程 golden 由发布门禁单独强制执行并通过 |
| Svelte/TypeScript | 0 错误、0 警告 |
| UI 单元测试 | 22/22 通过；覆盖弹窗键盘交互、月份/日期联动和链接型安全降级文案 |
| UI 生产构建 | 通过 |

默认忽略项不能替代发布验证。15 项中，图片引擎、扫描 PDF 引擎和生产 worker 端到端三项合成 OCR golden 已由发布脚本显式调用并通过；PDF 内嵌铁路 XBRL 私有聚合测试按授权单独执行并精确命中 14/14，链接型邮件私有聚合测试也按本次授权离线执行并命中 3/3、其他标签误报 0。普通构建仍保持这些测试默认忽略，其余真实数据和外部系统验证须逐项授权。

## 真实 QQ 只读与解析证据（2026-08-23；2026-08-24 重检）

- 固定账号掩码 `879***187@qq.com`、范围 `[2026-06-01, 2026-07-01)`、城市北京；仅使用 IMAP PEEK 读取。读取前后 UIDVALIDITY、范围 UID、FLAGS、大小和 Message-ID 摘要指纹一致。当前候选构建后的 2026-08-24 独立重检仍为 69 封、115 个附件、105 个候选、拉取/解析失败 0，前后 FLAGS 与内容集合指纹不变。
- 扫描 69 封邮件，69/69 原文读取成功；63 封含附件。直接 MIME 附件 101 个，ZIP 等展开后逻辑附件 115 个；抓取、解码和落盘过程无技术失败，私有原件均保留在 Git 仓库外。
- 邮件级预测盲标完成：57 封含有效发票附件、3 封仅含辅助材料、3 封链接型、3 封通知型、3 封非发票。原附件路径对可直接下载发票附件邮件 57/57 全部识别（precision 93.44%、recall 100%、F1 96.61%）；修复前严格发票相关邮件 precision 98.36%、recall 95.24%、F1 96.77%，主要缺口是 3 封链接型全部漏检。LINK-001 修复后，对同一冻结样本离线复验，3/3 个链接型全部生成安全提示，其他 66 封误报 0；验证过程网络请求 0。该能力只提示人工下载与本地导入，不把链接型邮件算作附件下载成功。
- 文件级盲标共 115 个逻辑附件；按内容去重后 110 个：有效发票 84、辅助材料 17、无效/损坏 9。
- 高召回预筛选器对 84 个有效发票全部召回。单看预筛选，严格“有效发票”口径 precision 80.91%、recall 100%、F1 89.45%；“发票或报销辅助材料”口径 precision 94.55%、recall 98.11%、F1 96.30%，无效文件预筛拒绝率为 33.33%。该层只决定是否进入解析，不代表产品最终接受。
- 105 个去重候选进入解析；合并已冻结的 OFD、PDF 与图片修复覆盖层，并增加开票结果/扫码下载页面的双锚点拒绝后，最终产生 84 个结构化结果，全部属于有效发票。严格“有效发票”最终判定 TP=84、FP=0、TN=26、FN=0，precision/recall/accuracy 均为 100%；其中 9/9 无效或非发票被最终拒绝。有效发票端到端解析覆盖 84/84：XML 8/8、OFD 21/21、PDF 54/54、PNG 1/1。旧报告把修复前的“16 个有效 PDF + 1 个有效 PNG”错误合并写成“17 个有效 PDF”，本次已按文件扩展名重新核对并纠正。上述比率仅适用于本批 110 个去重附件的文件级最终判定，不代表完整字段准确率或未知分布表现。
- 来源需求盲标显示 84 个有效发票中 83 个具有结构化/文本直读能力，1 个需要 OCR；本轮产品路径已覆盖这两类来源。
- 84 份有效发票的号码、日期、价税合计共 252 个核心字段槽位已完成最终真值。207 个槽位由产品输出盲化后的结构化证据或至少两个独立文本提取来源一致确定；其余 45 个槽位覆盖 22 份原件，先在不显示产品预测的页面人工读取，再在第二阶段逐项对照。对照发现 1 份运单明细 PDF 把运单号和寄件日期误作发票号与开票日期；修复后 252/252 匹配，84/84 发票三字段全匹配，DATA-001 关闭。该 100% 只描述本次冻结样本，不代表未知分布或跨邮箱、跨月份表现。
- 在不重新连接邮箱的前提下，对最终产品预筛候选集按稳定路径排序取前 50 个，用当前生产解析器执行离线聚合测试：JPG 1、OFD 12、PDF 36、XML 1；39 个有效发票全部产生结构化结果，8 个辅助材料、2 个非发票和 1 个损坏文件全部拒绝。逐文件累计 30.285 秒，p50 21 毫秒、p95 380 毫秒、最大 17.767 秒；低于 300 秒参考机门槛，测试体 30.400 秒，含 Cargo 启动的 wall time 35.951 秒。完整 105 个候选累计 94.427 秒：84/84 有效发票成功，15 个辅助材料、3 个非发票和 3 个损坏文件全部拒绝；接受/拒绝状态和成功结果 JSON 相对最终基线均无变化。
- 性能修复只在全部结构化/文本解析失败后，且可读文本同时含“行程单”、已知网约车平台、但不含发票/税务锚点时，把材料归为人工核对辅助材料并跳过 OCR；前 50 个命中 8 个，完整集命中 13 个。该规则不泛化到其他文本 PDF，不改变真实有效发票结果。PERF-001 已关闭。当前候选在参考机离线运行冻结 105 文件，100 ms 采样的进程树峰值工作集为 594,636,800 B，当前 Release OCR worker 峰值为 476,459,008 B；84 成功/21 拒绝保持基线，私有路径、文件名、QQ 邮箱和长数字日志命中均为 0。桌面解锁后，对最终主程序执行严格 packaged 空闲 15 秒基线：程序及自身 WebView2 进程树峰值 541,495,296 B，主程序 54,845,440 B，单个 WebView2 子进程最高 154,071,040 B，最多同时观测 6 个 WebView2；程序目录未变化、私密令牌命中 0、脚本退出码 0。先前锁屏会话的 `resource-in-use` 失败证据保留为脚本能拒绝假阳性的负例，不再是当前解锁会话的待办。真实 packaged QQ 批次也已由 UI 完成，但最低配置和第二台物理机仍未验证。脱敏证据见 `real-private-50-performance.validation.json`、`real-private-105-memory.validation.json`、`packaged-idle-memory-final-path-hardening.validation.json` 和归档负例。
- 14 个修复前失败的铁路电子客票 PDF 均在 PDF `EmbeddedFiles` 名称树中携带 XBRL/XML。新增解析器在文本与 OCR 前读取 `ElectronicInvoiceRailwayETicketNumber`、`DateOfIssue`、`Fare` 等结构化字段，14/14 进入 L0，非目标 PDF 误命中 0；有效 PDF 覆盖由 38/54 提升到 52/54。解析器限制 PDF 25 MiB、名称树节点、附件数、压缩输入、单份/总解压 XML，并对重复载荷去重、关键字段冲突拒绝自动选择；合成的 Flate、重复、冲突、非发票 XML 和解压膨胀回归全部通过。聚合证据见 `real-qq-pdf-embedded-xbrl-fix.validation.json`。
- 其余两个有效 PDF 分别通过严格标签模板恢复：运单明细仅在“运单明细、运单号码、寄件时间、开票时间、发票号码、发票总金额”六锚点同时存在时启用，并要求唯一 18–24 位发票号、开票日期与号码同一文本行、总额带固定币种单位；双语酒店账单只在“账单号码/Invoice No. + 打印日期/Print Date + 总计/Total”三锚点同时存在时启用，并要求标签前/后相对字段与唯一带时分秒日期。完整真值复核后的 105 份回归仍为 84 成功、21 拒绝，相对修复前仅目标运单明细的号码和日期各变化 1 次，无新增或回退。聚合证据见 `real-qq-delivery-field-fix.validation.json` 和 `real-qq-final-field-agreement.validation.json`。
- 图片解码改为按文件魔数识别格式，不再因匿名化或来源扩展名与实际 PNG 字节不一致而失败；有效 PNG 由 0/1 提升到 1/1，并有扩展名不一致的常规回归测试。税务/开票 App 的结果页即使含号码、日期和金额，也只有同时命中“开具结果/开票成功”等状态锚点与“扫码下载发票/继续开票”等操作锚点才拒绝；两张真实非发票结果页均被产品 worker 拒绝，单独出现“开票成功”的合成真实发票版式不被误拒绝。PDF 每条文本解析尝试仍单独隔离第三方库 panic 并继续 OCR；真实批次中两个非有效 PDF 的第三方 panic 被捕获，批次完成。OFD 无文本金额标签场景的兜底严格限定在 `total_amount`；有效 OFD 21/21 解析，回归测试证明该规则不会复用于发票号、税额、税率或名称。
- 仓库内只保存上述脱敏聚合和掩码，不保存授权码、邮件正文、附件、票号、金额或企业名称。
- 链接型交付：MIME 解析阶段只在正文同时含 HTTPS 和明确的发票下载/查看语义时产生布尔提示，不把主题、正文或 URL 传入流水线或检查点。邮件没有可直接处理附件时显示安全降级错误；混合批次在完成页保留数量提示。产品永不自动打开、复制或下载正文 URL，用户须在邮箱客户端核对发件人和域名后手动下载，再从“本地文件”导入。合成安全测试、EML/重复附件边界、恢复检查点、UI 文案和冻结真实样本 3/3/0 已通过；聚合证据见 `real-qq-link-only-hint.validation.json`。

## 本轮新增回归覆盖

- 可恢复流水线：任务与阶段状态进入 ledger schema v6；收集、解析、去重、归组检查点带哈希校验；启动标记中断任务；恢复复用已完成阶段；最终批次事务原子且幂等。
- 安全停止：运行中可由 UI 请求停止；当前文件结束后保存最近检查点、任务标记为 interrupted 并回到“可恢复任务”；邮箱路径在返回前仍强制核对只读 FLAGS，重复运行和重复停止请求被拒绝。
- 手动版本检查：只有用户点击“设置 → 版本与支持”才读取构建时固定的 HTTPS JSON；系统证书/代理、5/10 秒连接/总超时、64 KiB 上限、禁止重定向、产品/渠道/语义版本/哈希/下载页校验均已实现。当前内部 Alpha 未配置 URL，测试证明点击后返回“未配置”且零网络请求。
- Concur 收据邮件：ledger schema v6 持久化试发/确认/批量状态和幂等键；仅审批后附件、1–5 张原子预留、成功不重发、启动恢复 unknown、人工核对后解锁。页面完成三步交互，模拟 SMTP 成功/中断通过；内部 Alpha 构建、version.json 和 portable 门禁强制真实发送关闭，未访问真实 SMTP。
- 原件稳定化：本地文件、EML 和邮箱附件进入任务原件目录，文件名使用完整内容哈希；未知或篡改文件被拒绝。
- 输入资源边界：本地/EML/邮箱附件单文件 25 MiB、展开后最多 5000 个文件、总暂存量 500 MiB；目录重解析点被拒绝，EML/ZIP 展开共享同一计数与容量预算。
- 审核事务：发票字段编辑、重复项确认、人工新建/移动/合并归组、归组确认均生成前后快照；只允许草稿批次；可按相反顺序撤销。
- 提交门禁：疑似重复、签章无效、归组歧义或 requires_review 未清除时拒绝提交；已审计剔除项不参与批次统计、提交阻断和最终输出。
- 审核 UI：224 px 侧栏与分组/原件/字段三栏工作区；支持字段编辑、拆分提示、移动/合并、确认、历史、撤销、剔除/恢复和键盘快捷路径；Svelte 诊断为 0/0。
- packaged UI：最终主程序 `F449C597…AFD6` 使用全新仓库外数据根完成欢迎设置、真实 QQ 连接测试、北京常驻地、月份 `2026-06` 自动联动日期 `2026-06-01` 至 `2026-06-30`、只读邮箱收集、解析与审核草稿生成；最终批次 ID 1、发票 84、流水线 `review/completed`、最后错误为空，并保留 3 封仅链接邮件的安全人工降级提示。退出后凭据表为 0，UI 会话授权码已清除。随后只读复核仍为 69 封、115 个附件、105 个候选、失败 0，FLAGS 与内容集合证据和测试前逐字节一致。数据库中的 84/84 原件均存在并位于独立数据根，最长路径 235 字符、超过 259 字符 0；动态日志的完整邮箱、授权码、原件文件名、私有根路径、长数字和 ERROR 命中均为 0。脱敏证据见 `real-qq-ui-final-candidate.validation.json` 与 `real-qq-ui-final-post-flags.validation.txt`。较早候选的 150% 导航/设置、强杀恢复等证据仍按各自哈希保留，不归因于当前主程序。
- UI 缺陷闭环：本地来源为空时的 `errorMessage` 原先只在邮箱条件块内渲染，已改为所有来源均显示带 `role=alert` 的错误；创建批次弹窗增加初始焦点、窗口级 Esc、焦点循环和卸载后焦点恢复。修复后 Svelte 0 错误/0 警告、UI 17/17，真实 packaged 回归均通过；机器证据见 `packaged-ui-validation.validation.json`。
- packaged 强杀恢复：最新版主程序 `05CF02C8…C88D5` 在隔离 schema v6 数据根以仓库合成 XML 注入 production-shaped `created/running` 状态；精确强杀后数据库保持 running、`integrity_check=ok`，同根重启自动转为 interrupted，数据库仍完整。桌面自动化无法可靠观察最新版窗口，故本轮没有声称完成可见点击恢复，证据见 `packaged-current-recovery.validation.json`。较早主程序 `395B08BC…22EA` 曾对仓库外冻结 90 文件完成强杀、同根重启、可见“校验并恢复”和完整后端恢复，最终 70 个结构化结果、5 个带 SHA-256 检查点、数据库完整；该证据只证明旧可执行文件，不与最新版混同。当前源码另有 `pipeline_process_recovery` 等进程级强杀回归。
- packaged 无效数据根：较早主程序 `395B08BC…22EA` 把 `INVOICE_ASSISTANT_HOME` 指向仓库内合成普通文件后，显示“发票报销助手无法启动 / 数据目录路径不是文件夹”，Enter 确认后退出；窗口/进程无残留，哨兵文件类型/哈希和主程序哈希均不变。该项证明明确安全失败，不替代最新版复验、只读 ACL、低磁盘或无 WebView2。机器证据见 `packaged-invalid-data-root.validation.json`。
- 候选对应关系：当前主程序 SHA-256 为 `F449C597F80A510BECB1108716BED7FF13FA4D1F510DA9E49A6DD6A36F79AFD6`，worker 为 `DF6BC273C4A0AF2CE3F74A1A570040E7B4541B1417ED79C3D0F067BD02184AAF`；源码自动化、真实 QQ packaged UI、真实离线性能、严格空闲内存、portable 中文空格路径/数据隔离、OCR/DLL 对抗验证均通过。外层 ZIP 哈希只记录在同目录 sidecar 和仓库外部机器证据中，避免包内文档自引用。上一主程序 `05CF02C8…C88D5` 对应强杀后启动恢复；`395B08BC…22EA` 对应隔离首次设置、无效数据根、本地 EML/链接提示和私有 90 文件完整可见恢复；`36755B79…1420` 对应 150% 完整导航/设置与定向键盘证据。当前候选已关闭真实邮箱主流程缺口；UI-001 仍只跟踪尚未覆盖的完整缩放/故障/审核输出矩阵及第二台物理机。portable 机器证据见 `portable-verification-final-path-hardening.validation.json`。
- 隐私日志：在 `395B08BC…22EA` 的私有恢复日志中曾发现私有根路径文字 2 处、一个样本文件名 1 处；根因是解析失败警告直接格式化第三方错误。生产代码现只记录候选序号、扩展名和 `ocr_failed/ofd_failed/pdf_failed/xml_failed/parse_failed` 静态分类，并进一步移除启动日志中的数据根、日志目录和迁移快照绝对路径。专项测试及 Rust 519/0 全量门禁通过；最终主程序 `F449C597…AFD6` 的真实 QQ packaged 全流程日志共 1 个文件、34 行，ERROR、完整邮箱、授权码、原件文件名、私有数据根和长数字命中全部为 0。PRIV-002 已关闭，机器证据见 `real-qq-ui-final-candidate.validation.json`。
- 本次聚焦走查发现并关闭 TEST-001：生产 `builtin_hints` 在保持既有标签优先级后补充 `IssueDate/TotalAmount/TaxAmount`；新增生产 hints 直接解析合成 XML，以及 EML 展开→生产 hints→解析的端到端回归。两条自动化与上述当前 packaged 复验均精确命中号码、日期与金额，原合成 EML 已能独立进入产品流水线；该修复不改变真实 QQ 本批 84/84 解析结论。
- 数据库迁移：旧 v0 数据库连续升级到 schema v6；全部建表、索引、字段、版本号与启动恢复位于单一事务。v5→v6 在 DDL 后/版本提交前的故障注入证明原数据库文件逐字节不变、版本仍为 v5、原记录与完整性均保留，随后可正常升级。应用打开旧 schema 前先用 `VACUUM INTO` 创建并校验 `Data\migration-backups` 快照；当前 schema 不重复生成，未来 schema 在任何写入前拒绝。
- 数据版本与 packaged 回退：解析结果检查点保存 `formatVersion=1` 和 `parserVersion` 并拒绝未来格式；归组保存单一来源 `deterministic-v1`，备份保存格式/数据库/产品版本，输出保存格式/生成器版本。候选 EXE 在隔离数据根完成 v5→v6、复制已验证快照回滚、再次迁移到 v6，快照/主库完整、哨兵记录与程序哈希均保持，机器证据见 `packaged-migration-rollback.validation.json`。
- 幂等输出：稳定任务键纳入原件内容哈希；`.partial` 恢复、逐文件边车、最终 manifest 和原子目录发布；完成目录不覆盖。
- 输出内容：Excel、CSV、PDF 台账、A4 规范化打印合订本和原件 ZIP 共 5 项；原件归档保存精确字节与 SHA-256 索引，不包含本机源路径；数据库到输出包的回归证明剔除项不会进入 CSV、manifest 或原件 ZIP。
- 字体与打印：UI 内置 Source Han Sans CN 可变 WOFF2、IBM Plex Sans/Mono；A4 PDF 内置同版 Source Han Sans CN TrueType，不读取系统字体。官方提交、OFL 1.1 文本和资产 SHA-256 由 `third_party/fonts/fonts.lock.json` 校验。Poppler 对 2 页样本以 120 DPI 零警告渲染，确认标准 A4、中文无缺字/黑块、无重叠和裁切。
- CSV：UTF-8 BOM、中文、逗号/双引号转义和 Excel 公式注入防护。
- 原件预览：前端只按发票 ID 请求；后端拒绝符号链接、目录、超过 20 MiB 和非白名单格式；PDF/PNG/JPEG/WebP/GIF/BMP/XML 使用受控 Blob 预览，OFD 等格式明确提示暂不支持可视化。
- 离线 OCR：PNG/JPG/JPEG/WebP/BMP 直接解析；文本 PDF 原路径保持 L1，仅在定位文本和纯文本解析均失败后，将扫描 PDF 页面通过 Windows.Data.Pdf 在内存渲染并进入 L2 OCR。模型、运行时、来源、大小和 SHA-256 由 `third_party/ocr/ocr.lock.json` 固定并在启动前校验。
- OCR 安全与审核：单文件 25 MiB、单边 12000 像素、总像素 5000 万；扫描 PDF 限 1–5 页且每页渲染流不超过 25 MiB。L2/L4 或 L0/L1 置信度低于 0.90 时强制 `requires_review`，原因进入 evidence JSON。
- OCR 进程隔离：生产路径固定调用同目录 `invoice-ocr-worker.exe`，全局最多 1 个并发，每文件 45 秒硬超时；超时测试证明子进程被终止，Release 启动前按 portable manifest 校验 worker 哈希。父进程固定 worker 工作目录并移除 PATH/ORT_DYLIB_PATH；主程序与 worker 的 PE `DependentLoadFlags` 均为 `0x0800`，静态依赖只从 System32 解析；运行期仅登记已校验 OCR 目录。放置伪造 runtime/provider DLL 并设置恶意工作目录、PATH、ORT_DYLIB_PATH 后，图片与扫描 PDF golden 仍通过。参考机器生产 worker 图片/扫描 PDF 均值约 3.59/4.25 秒，峰值约 377/508 MiB；真实混合批次与最低配置仍待验证。
- OCR 回归：非隐私合成图片和扫描 PDF 均准确提取号码、日期、购销方、价税合计与税额；同时修复单列语义标签购销方错配和高 DPI 税额表头定位。可选税率仍可能误识别，不能作为自动通过字段。
- OCR 最终接受门禁：解析失败的候选只记脱敏警告并跳过，不进入去重、归组或批次草稿；开票结果/下载页使用状态与操作双锚点拒绝。本批产品 worker 离线复测为 1 张有效图片成功、2 张非发票结果页失败，聚合最终接受 84/84 有效发票、拒绝 9/9 无效/非发票。机器证据见 `real-qq-parse-coverage.validation.json`。
- 跨根备份：真实导出核心生成 ZIP，源数据根导入另一目标数据根；旧目标台账进入回滚目录，目标机器 accounts.db 保留。
- 进程恢复：独立子进程持久化 running 流水线后被操作系统强杀；新进程重开同一 ledger 后标记 interrupted，检查点仍完整。
- 安全清除：绝对路径、根目录、UNC、重解析点、manifest 路径穿越和未声明文件防护；一次性目录中的执行回归证明只删除产品/数据文件并保留同目录用户文件。
- portable 复验：verify-portable.ps1 固化外层/内部哈希、manifest、禁带文件、Authenticode、两个 PE 的 `DependentLoadFlags=0x0800`、中文空格路径启动和数据分离检查，可选输出 JSON 证据。

## 便携制品验证

- 文件名：`InvoiceAssistant-0.1.0-windows-x64-portable-UNSIGNED-INTERNAL-ALPHA.zip`。
- 外层 ZIP SHA-256 不写入包内文档，避免内容自引用；以 ZIP 同目录的 .sha256 sidecar 和 verify-portable JSON 证据为准。
- ZIP 共 33 项；portable manifest 记录 31 个发布文件，包内 `SHA256SUMS.txt` 校验 32 个文件，全部一致。
- 包含 README、发布说明、IT 审核摘要、隐私/协议草案、受控清理说明、Windows/OCR 验证记录、公开发布缺陷清单、历史夹具隐私处置、合成夹具清单、版本清单规范、`version.json`、SBOM、第三方许可证通知、字体 OFL 文本、ONNX Runtime 许可证/第三方通知及 OCR 资产锁定文件。
- 禁止文件扫描未发现 `.env`、邮件、数据库、日志、证书或私钥；真实授权码未进入包。
- 主程序与 OCR worker 的 Authenticode 状态均为 `NotSigned`，与内部 Alpha 文件名和说明一致。
- `version.json` 与机器证据确认 `concurSendEnabled=false`、`concurSendManualConfirmationRequired=true`、`concurSendRealTestApproved=false`、`dllSearchHardened=true`、`dependentLoadFlags=0x0800`；内部 Alpha 冒烟不连接 SMTP。
- 从 ZIP 解压到含中文和空格的隔离路径后可启动，窗口标题为“发票报销助手”，并可正常关闭。
- 隔离数据根目录生成 `accounts.db`、`ledger.db` 和本地日志；程序目录运行前后哈希完全一致。
- 更早候选主程序在 150% 缩放标准用户会话完成完整导航、设置与定向键盘回归，`395B08BC…22EA` 完成无效数据根、本地 EML 和链接型完成页安全提示，`05CF02C8…C88D5` 完成强杀后启动恢复。当前主程序 `F449C597…AFD6` 已在解锁桌面完成真实 QQ 首次设置、月份/日期联动、收集、解析、草稿生成、退出、只读 FLAGS 复核、原件路径与动态日志审计。仍缺当前主程序的 100%/125%/150% 完整缩放矩阵、可见恢复、只读 ACL/低磁盘/无 WebView2、完整审核/导出/备份及第二台物理机。
- 冒烟测试不连接邮箱、不导入真实发票；临时程序和数据目录测试后删除。

## 尚未完成的真实/人工证据

1. 历史 `fixtures/test-images` 不能证明为合成数据；当前工作树虽已隔离并由新门禁阻止回流，但 Git 历史、远端克隆/镜像、缓存和旧制品仍需受控清理与干净克隆复验。该 P0 关闭前不得公开发布。
2. 真实 QQ 的 IMAP 只读、邮件/文件级盲标、解析技术验证和最终 packaged UI 主流程均已完成；84 份有效发票的 252 个核心字段槽位已全部定标并在修复后 252/252 匹配，DATA-001 已关闭。最终候选集前 50 个累计解析 30.285 秒，已关闭参考机性能子缺陷 PERF-001；参考机真实 105 文件峰值工作集、最终 packaged 严格空闲内存和真实批次均已测得。最低配置和第二台物理机仍未完成。链接型安全提示在冻结样本达到 3/3、其他 66 封误报 0；自动访问或下载正文 URL 不是产品能力。
3. 2026-08-24 最终主程序已在 Windows 11 x64 解锁桌面完成真实 QQ 首次设置、北京常驻地、月份/日期联动、69 封邮件只读收集、105 个候选解析、84 份草稿生成和 3 封链接型提示；测试后 FLAGS 与内容集合未变化，授权码未持久化，84/84 原件可访问，动态日志敏感命中为 0。仍需当前候选完整导航/设置/恢复、100%/125%/150% 缩放、只读 ACL/低磁盘/无 WebView2、完整审核/导出/备份 UI 和第二台物理机。真实 QQ 授权码不得进入仓库日志或持久化证据。
4. 操作系统子进程强杀与 ledger 重开已由当前源码自动化覆盖；最新版 packaged 强杀/同根重启自动转 interrupted 且数据库完整，上一候选完成可见点击恢复。仍需最新版可见恢复、无 WebView2、只读 ACL、磁盘不足和两台物理电脑往返导入矩阵。
5. A4 合订本打印预览、原件对照、跨格式张数/金额/分组逐字段对账。
6. 清理执行核心已在一次性程序/数据目录完成破坏性回归；仍需从标准 portable UI 启动临时 helper、确认结果提示和 helper 残留行为。
7. 图片和扫描 PDF 离线 OCR 已完成并通过合成 golden；本批真实有效发票总体解析 84/84（100%），其中 XML 8/8、OFD 21/21、PDF 54/54、PNG 1/1，且 9/9 无效/非发票未进入最终结构化结果。本批 252 个核心字段全部匹配，但仍不能外推为未知分布表现；OFD 栅格化 OCR 尚未完成。
8. 最新版解析失败日志静态分类和启动路径脱敏已通过单测及真实 packaged 动态复验；路径、文件名、邮箱、授权码、长数字和 ERROR 命中均为 0，PRIV-002 已关闭。
9. Authenticode 签名、时间戳、正式 HTTPS 版本清单/下载页、外部 Beta 元数据、163 邮箱、Concur 试发与真实用户 UAT。

## 发布结论

- 可生成和验证文件名明确标注 `UNSIGNED-INTERNAL-ALPHA` 的内部候选包。
- 不满足公开 Beta 或正式发布条件。
- 真实数据、外发、签名和最终 Go/No-Go 继续按 `specs/mvp-release-baseline/tasks.md` 的阶段门禁执行。
