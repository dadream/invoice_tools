# 发布缺陷清单

> 候选：0.1.0 Windows portable internal Alpha
> 当前结论：非 Concur 功能已由产品负责人验收；完整 Concur MVP 与公开发布 No-Go
> 当前状态日期：2026-09-03；本文件下方长表保留 2026-08-26 缺陷快照，冲突时以本节为准

## 2026-09-03 权威开放项

| ID | 等级 | 状态 | 当前结论与关闭条件 |
|---|---|---|---|
| CONCUR-002 | P0 | Open | 本地映射、预检、冻结投影、幂等会话和禁用态已实现；真实租户的未提交报销单、费用填写、附件关联、回读、unknown恢复和防重复尚未验证。提供指定测试租户/授权/字段ID并逐项100%对账后关闭。 |
| PRIV-001 | P0（公开发布） | Open | 当前工作树和 portable 包不含旧夹具，但 `git rev-list --all --objects` 仍可达历史 `fixtures/test-images` 对象。由仓库管理员完成受控历史/远端/缓存/旧制品处置和干净克隆复验后关闭。 |
| SIGN-001 | P1（公开发布） | Open | 内部 Alpha 可未签名；任何公开 Beta/正式版必须使用可信 Authenticode 主体和时间戳，并复验签名后哈希。 |
| UPDATE-001 | P1（公开发布） | Open | 正式 HTTPS 下载页和版本清单未配置；内部手动分发不等于关闭。 |
| RELEASE-001 | P1（公开发布） | Open | 正式主体、版权、支持/隐私/安全渠道、协议地址、163兼容、外部Beta、最终签名RC和Go/No-Go责任人尚未全部完成。 |

## 2026-09-03 已关闭/重分类项

- 产品负责人确认除 Concur 真实集成外的 MVP 功能均已实际验证；UI-001、UI-003、UI-004、DATA-002、DATA-003、DATA-004、INPUT-001 的“等待产品验收”状态据此关闭。
- Excel 保存、材料打印 PDF、直接凭证首页、进度提示和全局页码已进入当前候选并通过自动/页面证据检查。
- QQ 真实只读样本、邮件台账、解析、分类、材料、去重、归组、费用审核和本地交付的结论只适用于已记录环境/样本，不自动扩展为 163、未知格式或公开发布承诺。
- CONCUR-001 的“收据邮件兼容路径”不再单独阻断非 Concur 功能 MVP；公开 Beta 如仍宣称该兼容能力，则并入 RELEASE-001/Concur 阶段门禁。完整草稿闭环仍由 CONCUR-002 跟踪。

## 2026-08-26 历史快照

| ID | 等级 | 状态 | 标题 | 影响与关闭条件 | 负责人/阶段 |
|---|---|---|---|---|---|
| PRIV-001 | P0 | Open | 当前 Git HEAD/远端跟踪引用仍包含未证明为合成的历史票据夹具 | 机器审计确认 `HEAD` 和本地 `origin/main` 跟踪引用均仍可达 20 个旧夹具（926,509 B、18 个唯一 blob）；工作树 20 个删除尚未提交。5 个本地产物 ZIP 禁止条目为 0，但 GitHub 实时 HTTPS 不可达、SSH 无公钥权限，远端/分支/标签/fork/缓存/Release 尚未盘点。需仓库管理员批准受控历史重写或新建干净仓库、强制更新远端、作废缓存/旧制品、干净克隆全门禁并由产品负责人确认后关闭 | 仓库管理员 + 产品负责人 / 公开 Beta 前 |
| PRIV-002 | P1 | Closed | 解析失败日志曾包含私有样本路径和文件名 | 生产警告已改为候选序号、扩展名和静态失败分类，并移除启动日志中的数据根、日志目录和迁移快照绝对路径。专项单测及 Rust 519/0 全量门禁通过；最终主程序 `F449C597…AFD6` 的真实 QQ packaged 流程日志共 34 行，ERROR、完整邮箱、授权码、原件文件名、私有数据根和长数字命中均为 0 | 开发 + 测试 / 2026-08-24 |
| UI-001 | P1 | Open | 真实 Windows UI/缩放矩阵证据不完整 | 当前主程序 `F449C597…AFD6` 已完成中文空格路径启动、数据隔离、严格空闲内存、真实 QQ 首次设置、月份/日期联动、69 封邮件收集、105 候选解析、84 份草稿、3 封链接型提示、测试后 FLAGS 复核、84/84 原件路径和动态日志验证。锁屏下 WebView2 `resource-in-use` 负例没有在解锁会话复现。仍需当前主程序可见恢复、100%/125%/150% 完整缩放、只读 ACL/低磁盘/无 WebView2、完整审核/导出/备份和第二台物理机 | 测试负责人 / Alpha 前 |
| UI-002 | P1 | Closed | 本地来源错误不可见且创建批次弹窗焦点/Esc 不完整 | 错误提示移出邮箱条件块；弹窗增加初始焦点、焦点循环、Esc 关闭和触发按钮焦点恢复。Svelte 0/0、UI 17/17、150% 标准用户 packaged GUI 回归通过 | 开发 + 测试 / 2026-08-24 |
| UI-003 | P1 | Ready for validation | 批次审核页原始数据区域持续闪烁且无法稳定加载 | 已切断 Blob URL 反馈环并改用 Windows 原生 PDF 页面渲染；提供重新加载、系统打开、打开所在文件夹。路径失效时可“重新关联原件”：同格式校验，优先核对旧 SHA-256；主发票无旧哈希时核对发票号/日期/金额，复制到稳定 DataRoot 后同步引用并写审核历史。2026-08-26 统一 Rust/前端/OCR/生产构建与免安装包结构验证通过；仍需真实批次 4 人工验收 | 开发完成，测试负责人验收 / 当前迭代 |
| UI-004 | P0 | Ready for validation | 批次审核与交付信息架构不符合双视图决策 | 已实现批次内仅“费用清单/归组”两个一级视图；导入、问题、材料和历史均收纳在双视图内部。阻断项清零后“完成审核”冻结版本化快照，再进入独立交付选择；Excel和Concur为两个独立主入口，返回修改会失效快照。2026-08-26 统一状态机、数据库、前端测试和生产构建通过；仍需人工 UI/缩放/返回上下文验收 | 开发完成，测试负责人验收 / 当前迭代 |
| DATA-002 | P0 | Ready for validation | 同批重复已能标记，但尚未落实“重复默认不计入总额”新口径 | 已建立 `duplicate_suspect/excluded/included` 强约束；疑似和确认重复默认均不计入总额、审核快照、Excel及Concur投影，只有用户明确“不是重复”才恢复。费用清单提供对比、归并副本、明确决定，页脚显示重复未计入张数与金额。数据库、审核快照和输出自动回归已通过；仍需真实批次 4 金额核对 | 开发完成，测试负责人验收 / 当前迭代 |
| DATA-003 | P0 | Ready for validation | 缺少发票单聚合和两阶段行程归组 | 已以 `ExpenseItem + InvoiceDocument` 聚合主发票、行程单、明细、其他材料和重复副本；待挂载材料不形成费用。归组只由铁路/航空/行程单锚点建立差旅，无锚点不建差旅；同一费用禁止属于多个组，移动后空组自动清除。旧批次可重新解析铁路/航空原件恢复路线并重算，全部结果要求再次人工确认并可撤销。迁移、重算、重叠行程和撤销自动回归已通过；仍需真实批次 4 归组验收 | 开发完成，测试负责人验收 / 当前迭代 |
| DATA-004 | P0 | Ready for validation | 单条费用核对缺少稳定本地费用模型，旧方案与Concur字段耦合 | schema v11 已建立版本化 `ExpenseItem`、位置、税明细、材料、来源及审核状态；单条核对只显示费用信息、票据信息、问题与依据。票据信息修改不静默覆盖稳定费用金额；Concur字段只在版本化映射、上传补充值、预检和冻结投影中出现，Excel不依赖Concur配置。迁移/快照/费用独立性自动回归已通过；仍需人工字段走查 | 开发完成，测试负责人验收 / 当前迭代 |
| INPUT-001 | P1 | Ready for validation | 本地导入UI仍把EML作为用户输入 | 活跃本地导入只接受 PDF/OFD/XML/图片文件或文件夹，文件选择器、拖放和文案不再暴露 EML；邮件文件只保留在邮箱内部采集实现和非活动历史组件。本地来源、格式边界和生产构建自动回归已通过；仍需人工拖放/文件夹导入验收 | 开发完成，测试负责人验收 / 当前迭代 |
| DATA-001 | P1 | Closed | 真实字段黄金真值曾不完整 | 84 份有效发票的号码、日期、价税合计共 252 个槽位已完成最终真值：207 个由产品输出盲化后的多来源/结构化高置信证据确定，45 个经 22 份原件逐项人工读取并在第二阶段精确对照。对照过程发现并修复 1 份运单明细 PDF 的运单号/寄件日期误取；修复后 252/252 匹配、84/84 发票三字段全匹配。该结论仅适用于本次冻结样本，不外推到其他邮箱、月份或未知版式 | 开发 + 测试 / 2026-08-24 |
| PERF-001 | P1 | Closed | 参考机真实 50 文件性能曾超过 5 分钟 | 稳定前 50 个累计 30.285 秒，完整 105 个累计 94.427 秒；39/39 与 84/84 有效发票成功，前 50 的 11 个及完整集 21 个非有效候选全部拒绝。当前候选离线 105 文件进程树峰值工作集 594,636,800 B，Release OCR worker 峰值 476,459,008 B；最终 packaged 解锁会话严格空闲 15 秒进程树峰值 541,495,296 B、主程序 54,845,440 B、最多 6 个 WebView2，程序目录未变化且私密令牌命中 0；真实 QQ packaged 批次也已完成。最低配置和第二台物理机仍由 M10.4/UI-001 跟踪 | 开发 + 测试 / 2026-08-24 |
| EMAIL-001 | P1 | Closed | 真实 QQ 邮件级准确率尚无冻结金标准 | 69 封已完成预测盲标；严格发票相关邮件 precision 98.36%、recall 95.24%、F1 96.77%，机器证据已保存 | 测试负责人 / 2026-08-23 |
| LINK-001 | P1 | Closed | 仅正文链接交付的发票邮件曾 0/3 被识别 | 仅在正文同时含 HTTPS 和明确发票下载/查看语义时生成布尔提示；不保存或访问 URL/正文。无可处理附件时明确引导用户在邮箱客户端核对发件人和域名、手动下载后从“本地文件”导入；混合批次完成页保留提示。合成安全回归、检查点恢复、Svelte 0/0、UI 22/22 通过；冻结真实样本 3/3 命中、其余 66 封误报 0，离线复验网络请求 0 | 产品 + 开发 + 测试 / 2026-08-24 |
| CLASS-001 | P1 | Closed | 高召回预筛选器曾把开票结果/下载页带入解析候选 | 预筛选仍以 recall 为先、无效文件拒绝率为 33.33%，但产品只接受成功产出结构化发票的候选；新增“开票状态 + 下载/继续操作”双锚点拒绝规则后，本批有效发票最终接受 84/84、无效/非发票最终拒绝 9/9，严格有效发票 precision/recall 均为 100%。更大邮箱、月份和未知分布仍需后续扩样 | 开发 + 测试 / 2026-08-24 |
| PARSE-001 | P1 | Closed | 本批真实有效发票自动解析覆盖曾为 81/84（96.43%） | 铁路 XBRL、严格运单/酒店 PDF 模板和按魔数图片解码完成后，本批有效发票 84/84：PDF 54/54、OFD 21/21、XML 8/8、PNG 1/1。完整真值复核又发现运单明细文本层先输出寄件栏导致首值误取；现仅在六个模板锚点全部命中时要求唯一 18–24 位号码、同一文本行日期和带币种总额。105 份全量回归的成功/失败集合不变，仅目标 PDF 的号码和日期两字段变化；最终 252/252 核心字段匹配 | 开发 + 测试 / 2026-08-24 |
| SIGN-001 | P1 | Open | 对外 PE 未签名且无时间戳 | 提供可信证书主体，主程序与 worker 使用同一主体签名并对签名后产物复验 | 发布负责人 / Beta 前 |
| UPDATE-001 | P1 | Open | 正式 HTTPS 版本清单和下载页未配置 | 代码/UI/安全校验已完成；提供正式域名，在最终签名 ZIP 发布后写入真实 SHA-256，执行有更新/无更新/超时与下载页复验 | 发布负责人 / Beta 前 |
| CONCUR-002 | P0 | Open | 核心“上传到Concur”真实适配器尚未解锁 | 本地侧已实现审核后入口、版本化企业映射、7个已知必填项与自定义字段预检、冻结投影、上传会话/外部ID/幂等键、报销单→费用→附件逐级状态、未知结果人工核对和局部恢复；能力门禁明确为禁用，不伪造外部成功。仍需产品负责人提供指定测试租户、登录/授权方式、稳定字段/选项ID和附件语义，才能实现并验证真实草稿创建、填表、附件关联与回读；最终不得提交 | 产品 + 开发 + 测试 / 核心功能发布前 |
| CONCUR-001 | P1 | Open | Concur 真实试发和租户行为未经受控验证 | 代码/UI/schema v6/模拟 SMTP/Alpha 关闭门禁已完成；提供测试租户、准确收件地址、已验证发件邮箱和逐次批准，完成一张试发、行为确认、部分成功、unknown 人工解锁及防重复批量复验 | 产品负责人 + 测试负责人 / Beta 前 |
| TEST-001 | P2 | Closed | 仓库合成 EML 曾未覆盖生产 XML 标签并集 | 生产 `builtin_hints` 在原标签优先级之后补充 `IssueDate/TotalAmount/TaxAmount`；新增生产 hints 直接解析合成 XML，以及 EML 展开→生产 hints→解析的端到端回归，均精确命中号码、日期和金额。完整门禁 Rust 519/15、UI 22/22、Svelte 0/0 通过；packaged 本地来源复验见 Windows 验证记录 | 开发 + 测试 / 2026-08-24 |

P0 不得豁免。该清单只记录脱敏事实，不附真实样本、授权码或票据内容。
