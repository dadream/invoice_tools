# 0.1.0 内部 Alpha 发布说明

## 变更

- 提供 Windows x64 免安装本地版，解压后可直接运行，程序与用户数据分离。
- 支持只读邮箱收集和本地票据导入，覆盖 PDF、OFD、XML、图片及扫描 PDF 离线 OCR。
- 支持发票解析、重复识别、费用核对、差旅归组及原件关联审核。
- 支持 Excel、Concur 本地映射预检和带全局页码的打印 PDF。
- 发布包采用最小文件白名单，并提供 SHA-256 校验和与 SBOM。

## 发布说明

本版本为未签名的内部 Alpha。Concur 真实草稿上传未启用，不适用于公开发布。
