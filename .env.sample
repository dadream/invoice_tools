# Invoice Collector Environment Variables Template
#
# 使用说明：
# 1. 复制本文件为 .env.local：cp .env.sample .env.local
# 2. 填入真实值（16 位小写字母授权码，从 QQ 邮箱设置获取）
# 3. 保护权限：chmod 600 .env.local
# 4. 加载到 shell：export $(grep -v '^#' .env.local | xargs)
# 5. .env.local 已在 .gitignore 中，不会被提交
#
# 注意：本文件（.env.sample）是模板，可以提交；
#       .env.local 包含真实凭证，绝对不能提交或分享

# QQ 邮箱 IMAP 授权码（不是账号登录密码）
# 获取方式：QQ 邮箱 → 设置 → 账户 → POP3/IMAP/SMTP 服务 → 生成授权码
# 格式：16 位小写字母（例如 abcdefghijklmnop）
INVOICE_IMAP_PASSWORD=your-16-char-authorization-code-here
