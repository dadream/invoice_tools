/target
fixtures/samples/
.env
*.eml

# Local environment with real credentials (never commit)
.env.local

# OCR model files (large binary files)
/models/*.onnx
/models/*.txt
/models/*.traineddata

# Vendored ONNX Runtime dynamic library (16MB, see models/README.md)
/vendor/
