# 实施任务

- [x] 1. 建立 schema v13 和历史迁移
  - 新增收集任务、邮件、附件和批次来源快照表。
  - 复制旧批次邮件台账并保留原数据。
  - _Requirements: R1, R7, R8_

- [x] 2. 实现独立收集状态机和持久化材料库
  - 收集阶段只做邮件/附件来源分类和安全存储。
  - 修复检查点白名单与规范化路径恢复问题。
  - _Requirements: R2, R3, R6, R7_

- [x] 3. 实现收集任务 API
  - 创建、列表、详情、启动、处置、补充文件和完成审核。
  - _Requirements: R1, R2, R3, R6_

- [x] 4. 实现批次来源快照和处理入口
  - 选择任务/附件，建立不可变快照并启动现有解析流水线。
  - 保留跨批次使用提示和来源追踪。
  - _Requirements: R4, R5_

- [x] 5. 实现独立邮件收集 UI
  - 主导航、任务列表、新建任务、详情双栏和审核操作。
  - _Requirements: R1, R2, R3_

- [x] 6. 重构批次导入 UI
  - 移除批次邮箱日期表单。
  - 增加收集任务三步选择器和本地导入入口。
  - 移除批次内完整邮件台账，保留来源摘要。
  - _Requirements: R4, R5_

- [x] 7. 完成回归验证和测试包
  - 运行迁移、Rust、Svelte、Windows 和便携包验证。
  - 不启动程序，由用户执行最终 UI 验证。
  - _Requirements: R1-R8_

- [x] 8. 将收集任务详情重构为分组邮件表格
  - 全宽表格、三个互斥分组、日期倒序、日期只显示 `YYYY-MM-DD`、分页与返回状态保持。
  - _Requirements: R9_

- [x] 9. 实现独立邮件审核页和安全正文/链接读取
  - 按需读取纯文本正文、提取相关 HTTPS 链接、系统浏览器打开及 FLAGS 不变校验。
  - 同页完成材料补充、处置和重新打开。
  - _Requirements: R10, R11_

- [x] 10. 实现收集附件按需预览并完成验证包
  - 复用 PDF/OFD/XML/图片安全预览，失败时允许系统打开。
  - 完成 Rust、Svelte、Windows 和免安装包验证；不启动程序，由用户手工验收。
  - _Requirements: R9-R11_
